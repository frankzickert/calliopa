{
  "members": [
    {
      "extension": "calliopa-base",
      "nodeId": "node:calliopa-base.working",
      "type": "ext.skill",
      "content": {
        "conventions": {
          "atomicProposal": "One extension change is one proposal group: every staged revision and relation belongs to the same group and is accepted or rejected as a unit. Never trickle per-file proposals; a half-applied extension state must never be materializable.",
          "humanWritesCostAConfirmation": "Every human-class write from the shell parks at the kernel bridge and waits for a confirmation on the kernel's own origin — no human mutation travels from the app origin to CCGW. Budget one cross-origin confirmation per write. That is proportionate for occasional truth like creating a channel and disproportionate for continuous editing, so a surface that writes on every keystroke needs a different shape than one that writes on a button.",
          "proofsAreNotProposals": "Never stage a proposal whose intended outcome is rejection. A proof-only group is indistinguishable from a real one at the review surface — the rationale is rendered as ordinary text — so after a run of proposals that all deserved acceptance, one that did not will be accepted by the same motion. Construct demonstrations so the refusal is the whole proof and stop there: a refused mutation stages nothing and opens no group. Acceptance is the one non-delegable human action in the architecture and its entire value is the attention behind it, so anything that produces frequent near-identical almost-always-correct proposals is spending that attention down.",
          "valuesTravelAsParameters": "How a statement says values, which the grammar will not let you guess:\n- Every value beyond a scalar travels beside the statement in the tool call's parameters field, which is one JSON object written as text — for example parameters: \"{\\\"bid\\\": \\\"art-x.b1\\\", \\\"runs\\\": [{\\\"text\\\": \\\"...\\\"}]}\". The statement writes $name where a value belongs; lists and objects have no literal form inside the statement itself, and runs: [{text: ...}] inline is a parse error every time.\n- Every $name in the statement must have a key in that JSON object. An empty parameters value beside a parameterized statement is always wrong, and resending a refused statement without filling it changes nothing.\n- A script is one or more statements separated by semicolons; CREATE ... RELATE ... in one breath is a parse error at RELATE. Aliases scope per statement but parameters are request-global, so one parameters object serves the whole script: the canonical block write is CREATE (n:Text {id: $bid, runs: $runs, order: $order}); RELATE childRef -[r:CONTAINS]-> parentRef, with bid, runs, order, childRef and parentRef all keys of the one JSON object.\n- RELATE endpoint refs resolve from parameters — <ref>NodeId first, then <ref> — and those values carry the node: prefix (childRef: \"node:art-x.b1\"), even though the id property inside CREATE content is bare. The ref names an existing node; the id property mints one.\n- When syntax is uncertain, calliopa_explain compiles a statement without executing it. Explain and correct rather than retrying a refusal unchanged — the diagnostics name what they want.",
          "whereRequirementsLive": "An extension's requirements and open work live in its own docs/ members (docs/system/**/*.md under the live-line protocol), read through calliopa_query like its code. An extension's changes are its own docs/changes/ members (BO_0254): docs/changes/<PREFIX>_NNNN_TYPE_<short-name>.md in its subtree, finished ones under docs/changes/completed/, each carrying its status in a Status: line from idea, draft, ready, wip, completed, rejected. They are written and revised the way every other docs member is — from a checkout, in the proposal that carries the code — and rendered read-only in the shell's Extensions section. A run working from the composer holds no checkout and so writes none. A run that changes an extension's behaviour also updates its docs in the same proposal group and keeps its change document's Status: line honest — wip when it claims ready work, completed when the work is done; only a person establishes draft or ready, and acceptance is what establishes any status. (BO_0201, BO_0254)",
          "workVocabulary": "Documents carry a decision vocabulary (BO_0244): a text block may have a kind, asserts claims, and reason-bearing relations connect claims. Two rules, whichever tool writes them: a relation is proposed with a reason naming the condition that connects its ends — never that they seem related — and with origin inferred or derived, since a run never declares; and a claim is drafted before a relation is anchored on a block that asserts none, which the relate item does for you when its end names a block with the claim's words. Propose these through propose_document_changes (items kind, claim, relate, reason, derive), never by statement, so review sees them as the editor would have written them.",
          "writeShapeFollowsFrequency": "Choose the write path by how often the operation happens, not by what it touches. Continuous editing stages candidates with no confirmation and one deliberate act to establish them; an occasional deliberate act — creating a channel, filing something into one — takes the confirmation directly, where the ceremony is proportionate. Both are the same architecture; only the frequency differs, and reaching for one shape everywhere makes typing intolerable or truth too cheap."
        },
        "goal": "Stage work into Calliopa's graph as one reviewable proposal, whatever the reader's intention was.",
        "id": "calliopa-base.working",
        "outputContract": "Zero or one open proposal group. Zero is a legitimate outcome and must be said plainly rather than filled with a placeholder.",
        "permittedOperations": [
          "MATCH ... RETURN GRAPH",
          "CREATE",
          "SET",
          "RELATE",
          "RETIRE"
        ],
        "preconditions": [
          "The run is bound to one proposal group at a pinned dataRevision; nothing staged becomes truth without human acceptance.",
          "The intention's own skill, if the run has one, carries what to produce; this one carries how any of it is staged."
        ]
      }
    },
    {
      "extension": "calliopa-extension",
      "nodeId": "node:calliopa-extension.intention",
      "type": "ext.intention",
      "content": {
        "completionRule": "An accepted proposal group: the run stages one atomic change, a human accepts it, and the activity is over.",
        "id": "calliopa-extension.intention",
        "label": "Extend",
        "marks": "extend"
      }
    },
    {
      "extension": "calliopa-extension",
      "nodeId": "node:calliopa-extension.create-extension",
      "type": "ext.skill",
      "content": {
        "conventions": {
          "authoringRoutes": "Two routes produce the same graph shape; pick one per change and never mix them. (1) Direct CCGW staging — what the examples below show: you write CREATE/SET and stage every partOf and dependsOn relation yourself. (2) The materializer round-trip — kernel checkout into a working tree, edit files, kernel commit — which derives partOf from the tree layout and dependsOn from the manifest dependencies map, so you never write a relation by hand. Human and agent authoring default to the materializer route — the kernel workspace tools are that route for a run; a Block that does not materialize (ext.skill, ext.settings, ext.migration, ext.blocktype, ext.relationtype, ext.intention) is reached by route 1, or carried by a members sidecar on kernel commit --members.",
          "binaryAssets": "Binary files travel: a binary in the diff becomes an ext.source Block whose code is a blob reference, the bytes uploaded through PUT /v1/blobs before the referencing mutation and hash-verified. The shell's favicon, PNGs and font ship this way.",
          "breakingChanges": "Tightening an established definition is gated; loosening it is free. Adding a required property, adding a unique property already duplicated, narrowing permitted children, enabling singleOpenCandidate, renaming, or removing a definition are breaking *when the type has established content* — and only then, so the same change is refused on a populated type and permitted on an empty one. A breaking change needs an ext.migration Block in the same group naming the types it covers, with a rationale and the operations that transform existing content travelling alongside; its authority is the human review of that group, not machinery. Removing a required property needs none, because requiring less invalidates nothing.",
          "capabilities": "The manifest capabilities object carries renders — the surface kinds the extension renders, as plural nouns: routes, views — and ccgwScopes, the CCGW operations its skills need, absent when the extension reads nothing. Nothing validates the shape, so a misspelled capabilities key builds clean and is never noticed.",
          "checkWhereEnforcementRuns": "A declaration is only enforced where something reads it. permittedChildTypes is honoured by the pages layer, and the pages layer is mounted in the production cell rather than the dogfood app — so in a dogfood deployment the declaration is real, resolved, and unenforced. Before relying on any declared constraint, find the code that acts on it and confirm that code is reachable from where the writes happen. When it is not, the surface owns the constraint and the gap belongs in the record.",
          "crossExtensionImports": "The shell imports its extensions and an extension may import the shell's lib and server modules by the ~/ alias; the tree is one pnpm package, so every import resolves and builds. Nothing enforces the manifest's dependsOn against those imports — declare what you import anyway, because the dependency edge is what makes the relationship traversable in the graph.",
          "declaredBaseline": "On the direct CCGW route the mutation must carry dataRevision — the pin the change was computed against. An auto-created proposal group records it as its baseDataRevision, and acceptance conflict-checks against that base. Omit it and the group bases at 0, so acceptance refuses with mutation_resolution_failed conflicts with truth that moved past its base revision 0 — a refusal that names a conflict but is really a missing baseline, and that only appears at acceptance, long after staging looked successful. The materializer route passes the checkout pin for you, which is why this trap belongs to route 1 alone.",
          "definitionShape": "Definition fields are data, not prose. In an ext.blocktype or ext.relationtype Block, requiredProperties, uniqueProperties, permittedChildTypes and runProperties are lists of property-name strings; block and singleOpenCandidate are booleans. A string where a list belongs parses to nothing at all — uniqueProperties: \"name among active channels\" declares no unique property and the rule silently never fires, which is far worse than a refusal because the definition still reads correctly to a human. Put the qualifier in a separate descriptive field, never inside the declaration. The same rule governs the manifest: dependencies is a map of extension id to semver range, capabilities is an object, and category is exactly bundled or individual.",
          "dependencyReferences": "A dependency on another extension is a dependsOn relation from the depending manifest to the dependency manifest, with the semver range recorded in the depending manifest dependencies map keyed by extension id — the range is content because a relation stores no properties, and the relation is what makes the dependency traversable. On the materializer route kernel commit derives the edge from the map; on the direct route you stage it. A dependencies entry naming an extension with no manifest, established or added by the same change, is refused. Two extensions added together may depend on each other. The range is checked twice: the registry scan fails a build whose tree holds a version outside a declared range (dependency_out_of_range), and the kernel refuses a version flip or an activation that would serve one before writing it (BO_0219). A version is the manifest version label at the subtree's last established state under it: bump version when you want a state of the subtree to be a version a person can serve from the extension view; two revisions under one label are one version, its last state.",
          "deploymentIsSeparate": "Acceptance and deployment are two acts. Accepting a proposal establishes truth at a new dataRevision; the kernel keeps serving the promoted release pin until kernel pin --to head moves it, building the candidate before it promotes. An accepted extension is therefore not yet a running one, and nothing warns that the two have diverged — verify the surface appears rather than inferring it from acceptance.",
          "derivedFiles": "Files matching *.gen.* are build output, never content, and the commit path skips them. The tree root's node_modules/, dist/ and server/ are the build's derived directories and are never committed either; a nested src/server/ is ordinary source.",
          "docs": "An extension carries its owner's knowledge beside its code, as ext.source members under docs/: docs/system/<area>/<topic>.md under the live-line protocol — a list item's marker is its commitment (* fixed, - mutable), its checkbox its state ([ ] open, [x] claimed, none is truth), a line beginning 'Functional question' awaits the owner, and a task identifier (XX_0000_000) ties a line to the change that made it true. docs/system/system.md's Purpose section is what the extension is for; README.md is the fallback; the skill's goal is the last resort. An extension's changes are its own docs/changes/ members (BO_0254): docs/changes/<PREFIX>_NNNN_TYPE_<short-name>.md in its subtree, finished ones under docs/changes/completed/, each carrying its status in a Status: line from idea, draft, ready, wip, completed, rejected. They are written and revised the way every other docs member is — from a checkout, in the proposal that carries the code — and rendered read-only in the shell's Extensions section. A run working from the composer holds no checkout and so writes none. The shell renders the system docs as a navigable network for the owner, so a change to an extension is not complete until its docs say so: convert the [x] lines you claimed into truth, add [ ] follow-ups for what remains, keep the identifiers, and write the change document's Status: line in the same proposal. Read the docs before changing the code; they are where the requirements live. (BO_0201, BO_0254) An extension may be started from the shell's Extensions section with a manifest and a docs/system/system.md alone — its purpose under Purpose, version 0.1.0, individual, no entrypoint, nothing to serve (BO_0224); the first topic names the extension's prefix, and the first code arrives through this skill as a proposal. An extension can leave an instance as one file — the kernel's export: its tree — its change documents among it — and its members — and enter another as a proposal the owner accepts, arriving switched off; ext.settings, activation and pins never travel, and the archive's category lands as individual.",
          "domainVocabulary": "A domain vocabulary is ext.blocktype and ext.relationtype Blocks in an extension of its own, depending on ui.shell — never inside the shell, which stays domain-neutral. Neither Block materializes, so the definitions are staged through direct CCGW or a members sidecar while the extension's sources go through the materializer; that is one rung, two routes, and they must not be mixed inside a single change. Declare uniqueness with uniqueProperties and let the lifecycle carry the qualifier: declared uniqueness is scoped to established revisions, so retiring a node frees its unique values and a \"retired\" or \"archived\" flag kept in content does not, because a flagged node stays established and keeps claiming them.",
          "draftingSurfacesReadThroughTheirDraft": "A surface that stages before establishing has two halves, and the staged half is invisible by design. Staged content is a candidate and does not resolve at head, so every read in a drafting surface must carry the draft’s overlay — the list that finds unoffered drafts, and the read that shows what was just written. Getting this wrong does not produce an error: it produces a dead end, where the author is told to offer a draft they cannot see or reach. Verify that the author can see what they wrote, not only that the write succeeded.",
          "featureExtensions": "A feature extension carries code, not values. The first is settings: src/extensions/settings/ holds the view (settings.tsx), the server modules (server/connections.ts, server/connection-tests.ts, server/adapters.ts) and the feature's migrations (migrations/*.sql, one sequence with the shell's own migrations/ ordered by file name and applied by the tree's serve entry). The shell keeps the thin route files under src/routes/api/settings/ importing from ~/extensions/settings/server/…, and its manifest declares dependencies {\"ui.shell\": \">=0.1.0\"}. Values the feature stores stay where the feature keeps them — the shell's encrypted connection store — never in ext.settings Blocks, which are plain graph content.",
          "freshNamesAreForced": "A graph definition may never shadow a code-registered type, so the frozen vocabulary’s names are unavailable: Page, Paragraph, Note and Document are all taken. Reincarnating a model therefore means fresh names carrying proven mechanics — which is the intended posture anyway, but the refusal enforces it whether or not you meant it.",
          "migrations": "The tree owns its schema. scripts/lib/migrations.mjs reads migrations/ and every src/extensions/<id>/migrations/ as one sequence ordered by file name, records each in schema_migrations, and the serve entry applies pending ones before it listens — except under the promotion gate's probe (CALLIOPA_SERVE_PROBE=1), which never migrates. Number a new migration after the highest existing one across both places.",
          "naming": "The manifest id is the extension id — lowercase letters and digits, starting with a letter, in parts joined by a dot or a hyphen, exactly ^[a-z][a-z0-9]*([.-][a-z0-9]+)*$, the rule the kernel's create enforces (BO_0224) — and must equal its directory name under src/extensions/ — the commit path refuses a mismatch. Source Blocks are <extId>.<path>. A contribution Block, if one is ever staged, carries a content id equal to its file name.",
          "orderingLivesOnTheChild": "Ordering is a content property on the child block, never on the containment relation, because a Relation stores no properties — a core-model fact, not a design choice. Use a base-36 fractional string compared as an ordinary string, so inserting between two siblings mints a key between theirs and writes one block instead of renumbering the set.",
          "rulesTheGraphCannotExpress": "Some settled rules have no home in the meta-schema and end up enforced in surfaces: which node types are ineligible as a relation’s endpoints, and permitted children wherever the enforcing layer is unreachable. Record each one where the rule is written rather than letting the surface be its only statement, because a rule living only in a surface is a rule the next surface will not know.",
          "shellShape": "The shell of this graph is calliopa-app, imported as ui.shell (BO_0200): a Qwik City application with file-convention routes under src/routes/ and compiled-in views (src/lib/views.ts), root-mapped as a whole. There is no runtime registry, no generated route map and no ext.uicontribution contract in this shell — the kernel still validates contribution Blocks on commit, but nothing reads them. An extension reaches the user through the shell's own source: the shell imports the extension's modules by path (~/extensions/<extId>/…) from a route file under src/routes/, a view entry in src/lib/views.ts, or a server module — so a new feature extension is one proposal that adds the subtree and the import lines in the shell, both root-mapped by the same kernel commit.",
          "structuralVocabularies": "A vocabulary with structure — containment, ordering, permitted children — is a different problem from a vocabulary of flat types. Reuse the code-registered CONTAINS relation rather than declaring a fresh containment type: the block machinery that enforces permitted children and nesting bounds resolves block types through the effective schema, so fresh graph-defined types inherit it, but every containment operation is written against CONTAINS. A fresh containment relation leaves permittedChildTypes declared and inert, which is the silent-no-op failure this system works hardest to avoid.",
          "subtreeShape": "One ext.manifest Block is the subtree root. Every member Block — ext.source, ext.blocktype, ext.relationtype, ext.skill, ext.uicontribution — carries a partOf relation to that manifest.",
          "uiContributionContract": "Retired with the calliopa-app shell. ext.uicontribution Blocks are still a valid member kind the kernel validates on commit (kind route, panel or command; content id equal to the file name), but this shell reads none of them; declare UI through the shell's own imports instead (shellShape).",
          "wholeTreeCommits": "The materializer diff is whole-tree: kernel commit stages everything that differs from the lockfile, not the subtree you were working in. Running the repository's own formatter while authoring an extension can therefore rewrite another extension's sources and stage them alongside your work — observed rewriting className to class across ui.shell. Check kernel status before every commit and confirm the file list is only yours; a foreign file in the list is a signal to stop, not a detail to accept."
        },
        "examples": [
          {
            "description": "A feature extension the shell imports: the settings extension's shape. One proposal adds the subtree and the shell's import lines.",
            "manifest": {
              "capabilities": {
                "renders": [
                  "views"
                ]
              },
              "category": "bundled",
              "dependencies": {
                "ui.shell": ">=0.1.0"
              },
              "id": "settings",
              "type": "ext.manifest",
              "version": "1.0.0"
            },
            "members": [
              {
                "code": "the view component, imported by src/components/shell/view-host.tsx as ~/extensions/settings/settings",
                "id": "settings.settings.tsx",
                "language": "tsx",
                "path": "settings.tsx",
                "type": "ext.source"
              },
              {
                "code": "the server module, imported by the route files under src/routes/api/settings/",
                "id": "settings.server/connections.ts",
                "language": "ts",
                "path": "server/connections.ts",
                "type": "ext.source"
              },
              {
                "code": "the feature's schema, applied in sequence with the shell's migrations/",
                "id": "settings.migrations/0006_connection.sql",
                "language": "sql",
                "path": "migrations/0006_connection.sql",
                "type": "ext.source"
              }
            ],
            "proposal": "one proposal group staging the subtree, its partOf relations, the dependsOn edge, and the shell's own files that import it — all produced by one kernel commit from the working tree",
            "relations": [
              "settings.settings.tsx partOf settings",
              "settings.server/connections.ts partOf settings",
              "settings.migrations/0006_connection.sql partOf settings",
              "settings dependsOn ui.shell"
            ]
          },
          {
            "description": "A domain vocabulary: two type definitions and their manifest, staged as one proposal. Note that every declaration is a list or a boolean and every human explanation lives in its own field.",
            "manifest": {
              "capabilities": {
                "ccgwScopes": [
                  "query",
                  "propose"
                ],
                "renders": [
                  "views"
                ]
              },
              "category": "individual",
              "dependencies": {
                "ui.shell": ">=0.1.0"
              },
              "id": "example.library",
              "type": "ext.manifest",
              "version": "0.1.0"
            },
            "members": [
              {
                "block": false,
                "description": "A shelf groups books. Names are unique among active shelves; archival retires the node, which frees the name.",
                "id": "example.library.Shelf",
                "name": "Shelf",
                "requiredProperties": [
                  "id",
                  "name"
                ],
                "type": "ext.blocktype",
                "uniqueProperties": [
                  "name"
                ]
              },
              {
                "description": "Membership from a book to a shelf. Many-to-many, versioned through ordinary relation validity.",
                "id": "example.library.ON_SHELF",
                "name": "ON_SHELF",
                "requiredProperties": [],
                "type": "ext.relationtype"
              }
            ],
            "proposal": "one proposal group staging the manifest, both definitions, and every relation below, accepted or rejected as a unit",
            "relations": [
              "example.library.Shelf partOf example.library",
              "example.library.ON_SHELF partOf example.library",
              "example.library dependsOn ui.shell"
            ]
          }
        ],
        "goal": "Create a new extension, or evolve an existing one, as a complete subtree of Blocks staged in exactly one atomic proposal through CCGW.",
        "id": "calliopa-extension.create-extension",
        "outputContract": "Exactly one open proposal group staging the complete extension change — manifest, member Blocks, their partOf relations, and any dependsOn relations — with rationale and provenance. No writes outside the proposal, no established status, no partial subtree that could materialize half-applied. On the materializer route the same group is produced by kernel commit, with partOf and dependsOn derived rather than written by hand; the resulting graph shape is identical.",
        "permittedOperations": [
          "MATCH ... RETURN GRAPH",
          "CREATE",
          "MERGE",
          "SET",
          "REMOVE",
          "RELATE"
        ],
        "preconditions": [
          "The extension meta-schema definition Blocks are established truth; read the ext.blocktype and ext.relationtype definitions before staging.",
          "The target extension id is decided and does not collide with an existing ext.manifest id.",
          "Every extension named in the manifest dependencies map has a manifest, established or added by this same change.",
          "The mutation context declares an agent identity scoped to one open proposal group; a skill execution never writes established truth.",
          "The extension's docs/ members are read before its code is changed: docs/system/**/*.md carry the requirements and open work, and docs/changes/*.md the changes and the status each stands at (BO_0254)."
        ]
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:BlockKind",
      "type": "ext.blocktype",
      "content": {
        "id": "BlockKind",
        "name": "BlockKind",
        "optionalProperties": [
          "words"
        ],
        "properties": {
          "id": "the declaration's identity; the node id is node:<id>",
          "value": "the value this declaration permits",
          "words": "how the value reads to a person, when it differs from the value"
        },
        "requiredProperties": [
          "id",
          "value"
        ],
        "semantics": "A value text.kind may carry. The slot is this extension's and it enumerates none: what a block may be said to be is declared by the extension that has something to say about blocks, as instances of this type attached to its own manifest by partOf (extension-model.md, Declaration By Instance). A declaration instance is not a member the export sees, so an individual extension's vocabulary lives in its instance's graph and travels with no release. Declared by documents under BO_0256_002.",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:RootPhase",
      "type": "ext.blocktype",
      "content": {
        "id": "RootPhase",
        "name": "RootPhase",
        "optionalProperties": [
          "words"
        ],
        "properties": {
          "id": "the declaration's identity; the node id is node:<id>",
          "value": "the value this declaration permits",
          "words": "how the value reads to a person, when it differs from the value"
        },
        "requiredProperties": [
          "id",
          "value"
        ],
        "semantics": "A value document.phase may carry, declared by instance as BlockKind is. The slot is this extension's; the phases are the decision extension's, so an instance holding none can propose no phase, which is correct rather than a gap. Declared by documents under BO_0256_002.",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:claim",
      "type": "ext.blocktype",
      "content": {
        "id": "claim",
        "name": "claim",
        "properties": {
          "id": "the claim's identity, a UUID the shell mints; the node id is node:<id>",
          "text": "the assertion in canonical words, as runs, distinct from the wording of the block that asserts it"
        },
        "requiredProperties": [
          "id",
          "text"
        ],
        "runProperties": [
          "text"
        ],
        "semantics": "An assertion with an identity of its own, made by exactly one text block through asserts. A block may assert several; relations anchor on claims, never on blocks, so a rewording of the block and a change of its meaning can be told apart (BO_0248 compares a claim's revisions). Not a block: it is not ordered and nothing contains it. Declared by ui.shell under BO_0244_006.",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:divider",
      "type": "ext.blocktype",
      "content": {
        "block": true,
        "id": "divider",
        "name": "divider",
        "optionalProperties": [
          "intention"
        ],
        "permittedChildTypes": [],
        "properties": {
          "id": "the block's identity, a UUID the shell mints; the node id is node:<id>",
          "intention": "the marking a reader gave this block, as on text",
          "order": "the block's place among its siblings, the same key text carries"
        },
        "requiredProperties": [
          "id",
          "order"
        ],
        "semantics": "A leaf block carrying no authored text and taking no editor: a horizontal rule in the reading order. It declares no run property, which is what makes split and merge meaningless on it rather than needing a rule of their own.",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:document",
      "type": "ext.blocktype",
      "content": {
        "id": "document",
        "name": "document",
        "optionalProperties": [
          "intention",
          "record",
          "phase",
          "supersededBy"
        ],
        "permittedChildTypes": [
          "text",
          "divider"
        ],
        "permittedValues": {},
        "properties": {
          "id": "the document's identity, a UUID the shell mints; the node id is node:<id>",
          "intention": "the marking a reader gave the whole document: the marks value of an established ext.intention declaration; unconstrained here because intention extensions are declared per instance and a permitted set in this declaration would refuse a marking the instance legitimately declares — the agent bridge refuses an unknown intention at run start",
          "phase": "the root's phase (BO_0249): proposed — a safe hypothetical environment, the default when absent; accepted — the wider system may rely on it; superseded — another root replaced it, named in supersededBy. The values are an established RootPhase declaration's, unconstrained here for the reason kind is (BO_0256_002)",
          "record": "the record slot carried over from the previous document type: names a record declaration; no permitted set, per the recorded non-enforcement of BO_0199_002",
          "supersededBy": "the id of the root that superseded this one; present only with phase superseded, written in the same mutation that accepts the successor (BO_0249)",
          "title": "the document's title"
        },
        "requiredProperties": [
          "id",
          "title"
        ],
        "semantics": "The one product-level document kind: a node with a title whose content is its ordered child blocks, held as CONTAINS relations from the document to each block rather than nested in its revision. A document is not itself a block: it is not ordered among siblings and nothing contains it. At most one open candidate per document. Declared by ui.shell under BO_0207_011; the previous gateway's TypeScript definition of the same type is retired with the gateway. Since BO_0254 an extension's changes are its own docs/changes/ members, not documents: a document is a document. Since BO_0249 a root carries a phase — proposed, accepted or superseded — and, superseded, names its successor in supersededBy.",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:policy",
      "type": "ext.blocktype",
      "content": {
        "id": "policy",
        "name": "policy",
        "optionalProperties": [],
        "permittedChildTypes": [],
        "permittedValues": {
          "rule": [
            "owner"
          ]
        },
        "properties": {
          "id": "the policy's identity; the node id is node:<id>",
          "rule": "who may establish a root under this policy: owner — the instance's owner and any human account the instance admits, the one rule today (BO_0249_006); named approvers, required reviewers and majorities are each their own change"
        },
        "requiredProperties": [
          "id",
          "rule"
        ],
        "semantics": "An acceptance policy a root references through an acceptedBy relation (material §27): acceptance policy belongs to the block, never to the route, and a child inherits policy only through an explicit relation. Declared by ui.shell under BO_0249_006 with one permitted rule, owner, so the hook is the relation the material asks for and not a policy engine.",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:relation",
      "type": "ext.blocktype",
      "content": {
        "id": "relation",
        "name": "relation",
        "optionalProperties": [
          "state"
        ],
        "permittedValues": {
          "kind": [
            "dependsOn",
            "supports",
            "contradicts",
            "qualifies",
            "constrains",
            "implements",
            "supersedes",
            "evidences",
            "opensQuestionIn",
            "affectedBy"
          ],
          "origin": [
            "declared",
            "derived",
            "inferred"
          ],
          "state": [
            "declared",
            "exercised",
            "needsReview",
            "orphaned",
            "retired"
          ]
        },
        "properties": {
          "id": "the relation's identity, a UUID the shell mints; the node id is node:<id>",
          "kind": "what the source claim does to the target claim, from the material's list; derives-from is the derivedFrom provenance edge, not a kind here",
          "origin": "declared by a person or a governed process; derived mechanically from accepted structure; inferred by a run — an inferred relation is a proposal until a person confirms it and never propagates pressure before",
          "reason": "why the source matters to the target, as runs, in the reader's or the run's words: the condition that connects them, which is what makes propagation precise",
          "state": "the relation's lifecycle: absent means declared; exercised once evaluated against a change; needsReview when its reason may no longer apply; orphaned when an end is gone; retired when intentionally ended. A relation ends by this state and is never RETIREd, so its record stays readable"
        },
        "requiredProperties": [
          "id",
          "kind",
          "reason",
          "origin"
        ],
        "runProperties": [
          "reason"
        ],
        "semantics": "A reason-bearing relation between two claims: a node, because a Relation stores no properties and the reason is what carries consequence (data-model.md BO_0029_003). Joined to its ends by source and target edges leaving this node, so staging one revises neither claim nor block. Proposed is the node being a candidate, not a state. Declared by ui.shell under BO_0244_006.",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:text",
      "type": "ext.blocktype",
      "content": {
        "block": true,
        "id": "text",
        "name": "text",
        "optionalProperties": [
          "role",
          "intention",
          "disposition",
          "kind"
        ],
        "permittedChildTypes": [],
        "permittedValues": {
          "disposition": [
            "keep",
            "pin",
            "resolved",
            "discarded"
          ],
          "role": [
            "paragraph",
            "h1",
            "h2",
            "h3",
            "quote"
          ]
        },
        "properties": {
          "id": "the block's identity, a UUID the shell mints; the node id is node:<id>",
          "intention": "the marking a reader gave this block, resolved own marking first, else the nearest marked ancestor, else the document's; the marks value of an established ext.intention declaration, unconstrained for the reason the document type records",
          "kind": "what the block is, in whatever vocabulary an extension declares for it: the value of an established BlockKind declaration. Unconstrained here, because the values are declared by the extension that owns them and this one has none to name (BO_0256_002)",
          "order": "the block's place among its siblings: a fractional key compared as a string, so inserting between two siblings mints a key between theirs and writes one block",
          "role": "the text's role; absent means paragraph, so an ordinary block stores no role property. Widening this set is an ordinary change; narrowing it is breaking, because established content may hold a value the narrower set forbids",
          "runs": "the formatted text as an ordered list of runs, each carrying text and its own marks and no offsets; the core canonicalizes runs on every write"
        },
        "requiredProperties": [
          "id",
          "order",
          "runs"
        ],
        "runProperties": [
          "runs"
        ],
        "semantics": "A leaf block of formatted text, containable through CONTAINS, ordered by its order key, split and merged as a list of runs. The marks a run may carry — bold, italic, strikethrough, code, and a link — are a property of runs, not of the block, so they are enforced by the run decoder and the editor rather than by a permitted set on this declaration.",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:acceptedBy",
      "type": "ext.relationtype",
      "content": {
        "id": "acceptedBy",
        "name": "acceptedBy",
        "semantics": "A root's acceptance policy: directed from a document to the policy node whose rule says who may establish it. Absent, the root is under the owner rule. Declared by ui.shell under BO_0249_006."
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:asserts",
      "type": "ext.relationtype",
      "content": {
        "id": "asserts",
        "name": "asserts",
        "semantics": "A text block asserts a claim; directed block to claim, one relation per claim, in the block's order. A block gets its claims when a relation is declared on it or a run drafts them. Declared by ui.shell under BO_0244_006."
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:derivedFrom",
      "type": "ext.relationtype",
      "content": {
        "id": "derivedFrom",
        "name": "derivedFrom",
        "semantics": "A block was derived from another block: directed from the derived block to a block it rests on, staged by the run that drafts it, so system-maintained from two accepted blocks is a fact the graph holds and provenance reads. Declared by ui.shell under BO_0244_006."
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:focuses",
      "type": "ext.relationtype",
      "content": {
        "id": "focuses",
        "name": "focuses",
        "semantics": "A document is focused work on a block: directed from the child document to the text block it elaborates, at most one document per block, the shell's rule. The block keeps its place in its parent; deleting the child closes the edge. Declared by ui.shell under CA_0047_001."
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:retired",
      "type": "ext.relationtype",
      "content": {
        "id": "retired",
        "name": "retired",
        "semantics": "Records that a block left a document's reading order without being deleted, directed from the document to the block. Retiring a block closes its CONTAINS validity and creates this relation in the same mutation; restoring closes this relation and creates a new CONTAINS. Closing containment alone would strand the block beyond any rooted read, so retirement is recorded as its own relation and the retired list is an ordinary read from the document. Containment itself is the core's CONTAINS relation and needs no declaration here."
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:source",
      "type": "ext.relationtype",
      "content": {
        "id": "source",
        "name": "source",
        "semantics": "A relation node's source claim, the assertion whose change may matter; directed relation to claim. Declared by ui.shell under BO_0244_006."
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:target",
      "type": "ext.relationtype",
      "content": {
        "id": "target",
        "name": "target",
        "semantics": "A relation node's target claim, the assertion that may be affected; directed relation to claim. Declared by ui.shell under BO_0244_006."
      }
    },
    {
      "extension": "documents",
      "nodeId": "node:ui.shell.documents",
      "type": "ext.skill",
      "content": {
        "conventions": {
          "changeDocuments": "An extension's changes are its own docs/changes/ members (BO_0254): docs/changes/<PREFIX>_NNNN_TYPE_<short-name>.md in its subtree, finished ones under docs/changes/completed/, each carrying its status in a Status: line from idea, draft, ready, wip, completed, rejected. They are written and revised the way every other docs member is — from a checkout, in the proposal that carries the code — and rendered read-only in the shell's Extensions section. A run working from the composer holds no checkout and so writes none. A document tool never writes one: create_document takes a title and a first block and nothing else. (BO_0254)",
          "claimsAndRelations": "A block asserts claims — its assertions in canonical words, each with an identity — and relations anchor on claims, never on blocks: a relate item names a source and a target claim by claimId, or a block by blockId with the claim's words to draft when the block asserts none; a block that asserts exactly one claim may be named alone. A relation is proposed with a reason naming the condition that connects its ends (cache reuse is safe only while the context is unchanged), never that they seem related, and with origin inferred or derived — a run never declares. A block's kind says what it is in the decision vocabulary (assumption, question, evidence, decision, …); a derive item says which blocks a block the same group inserts rests on. A relation ends by its state and is never removed. (BO_0244)",
          "commit": "A commit command is answered by a proposal, never by a write (BO_0249_012; material §46, rule 19): when the command says 'yes, this is the decision', 'treat this as the requirement', 'make this the accepted direction' or the like, stage exactly one phase item {kind: \"phase\", phase: \"accepted\"} on the document the command is about, with the rationale as one sentence of consequences drawn from the relations sourced on the document's claims and the judgements standing on its blocks — never a decision block, never a rewrite of the pinned synthesis. The shell draws that proposal as the transition card under the intent, and the reader's press on Establish is the one confirmation. This rule lives here because a person's run reads this skill whatever the document's intention; refine's conventions carry it as well. 'This' in a commit command is the document, never a block: a block the reader marked or referenced says where the decision is stated, so the phase item is the answer even then — mark that block's kind decision in the same proposal if it is not already, but a kind item alone answers nothing (found live 2026-09-15: two runs answered 'yes, this is the decision' with a marked block by a kind item and no phase).",
          "marks": "A run carries text and, when it has any, marks among bold, italic, strikethrough and code, and may carry a link. Marks are not declared in the graph's vocabulary; the editor's set is the one that renders.",
          "readerAnswersPerItem": "Each item is decided on its own in the editor. Keep an item to one intent, so a reader can accept the heading and reject the rewrite."
        },
        "examples": {
          "addAClaim": "propose_document_changes {documentId, rationale, items: [{kind: claim, blockId, text: [{text: 'Cached authorization stays valid for the request.'}]}]}",
          "addAHeadingAbove": "propose_document_changes {documentId, rationale, items: [{kind: insert, block: {kind: text, role: h2, runs: [{text: 'Heading'}]}, placement: {before: blockId}}]}",
          "commitADecision": "propose_document_changes {documentId, rationale: 'The reader said yes, this is the decision. Accepting would make two documents rest on its definition and reopen one requirement under review.', items: [{kind: phase, phase: accepted}]}",
          "commitADecisionWithAMarkedBlock": "propose_document_changes {documentId, rationale: 'The reader said yes, this is the decision, marking block #8 where it is stated. Accepting would …', items: [{kind: phase, phase: accepted}, {kind: kind, blockId: <#8>, blockKind: decision}]}",
          "dropAStrayDivider": "propose_document_changes {documentId, rationale, items: [{kind: remove, blockId}]}",
          "findAnExtensionsChanges": "the extension's docs/changes/ members, read from a checkout or GET /api/extensions/<id> — the Status: line of each says where it stands",
          "relateTwoClaims": "read_document {documentId} → find the claims → propose_document_changes {documentId, rationale, items: [{kind: relate, relation: {kind: dependsOn, reason: [{text: 'the cache is safe only while the request sees one revision'}], origin: inferred, source: {claimId}, target: {blockId, claim: [{text: 'A request sees one stable revision.'}]}}}]}",
          "reviseAReason": "propose_document_changes {documentId, rationale, items: [{kind: reason, relationId, reason: [{text: '...'}]}]}",
          "rewriteAParagraph": "read_document {documentId} → find the block whose runs carry the sentence → propose_document_changes {documentId, rationale, items: [{kind: replace, blockId, runs: [{text: '...'}]}]}",
          "sayWhatABlockRestsOn": "propose_document_changes {documentId, rationale, items: [{kind: insert, block: {...}, placement}, {kind: derive, blockId: <the inserted block>, from: [blockId, blockId]}]}",
          "setAKind": "propose_document_changes {documentId, rationale, items: [{kind: kind, blockId, blockKind: assumption}]}"
        },
        "goal": "Read and propose changes to the documents a reader writes in, through the kernel toolset's document tools, so a run's proposal lands exactly what the editor would have written and is reviewed per item.",
        "id": "ui.shell.documents",
        "outputContract": "One propose_document_changes call per coherent change to one document, with a rationale saying why. Report the group the tool answered and what it stages; never claim the change is accepted. A refusal envelope is an answer: read its diagnostics and adjust the items rather than retrying the same call. A commit command — 'yes, this is the decision', 'treat this as the requirement', 'make this the accepted direction' — is answered by one phase item {kind: \"phase\", phase: \"accepted\"} on the document, whatever block is marked or referenced: the marks say where the decision is stated, not what to change, and a kind item alone is the wrong answer (conventions.commit).",
        "permittedOperations": "list_documents to find a document by title, and an extension's changes by change — each row carries change and status for a change document; read_document for its blocks in reading order with their identities, kinds, roles and runs, and for a change document its extension and status; propose_document_changes with items of kind replace (runs and/or role for a block), insert (a new text or divider block at a placement), remove (a block leaves the reading order, retired rather than deleted), move (a block to a placement) and status (a change document's status, one of idea, draft, ready, wip, completed, rejected); create_document for a new document with a title, its first block and — for a change of an extension — change naming the extension and a status, idea unless said. Everything stages into this run's proposal group; nothing writes truth. The statement tools remain available for anything beyond documents. — and, since BO_0244, kind (a block's blockKind, none clears it), claim (add a claim to a block, or revise one by claimId), relate (a reason-bearing relation between two claims), reason (a relation's new reason) and derive (the blocks a block rests on). read_document answers each block's blockKind, claims, provenance and derivedFrom, and the document's relations with both ends. Since BO_0249, phase: the root's phase — {kind: \"phase\", phase: proposed | accepted | superseded, supersededBy only with superseded} on the document itself, refused on a change document, whose status is its lifecycle. An agent proposes a phase and never sets it; the reader establishes it from the transition card.",
        "preconditions": "The run holds the kernel toolset. The document to change exists at the run's pin; read it with read_document before proposing against it, because a replace or a move names a block by the identity that read answered."
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:assignment",
      "type": "ext.blocktype",
      "content": {
        "id": "assignment",
        "name": "assignment",
        "properties": {
          "channel": "the channel that takes the shape",
          "container": "the container the site declares that the shape is taken into",
          "id": "the assignment's identity, a UUID the extension mints; the node id is node:<id>",
          "parts": "the list of {part, slot}: each of the shape's parts and the slot of that container it is assigned to",
          "shape": "the shape taken"
        },
        "requiredProperties": [
          "id",
          "channel",
          "shape",
          "container",
          "parts"
        ],
        "semantics": "What a channel takes: one shape into one container the site declares, its parts assigned to slots. One per channel and shape, related to the channel by `takes` and to the shape by `of`; what it names is what the index read keeps when the site stops listing a key. PU_0003_001",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:binding",
      "type": "ext.blocktype",
      "content": {
        "id": "binding",
        "name": "binding",
        "optionalProperties": [
          "address",
          "number",
          "disclosure"
        ],
        "permittedValues": {
          "recordKind": [
            "deliverable",
            "item"
          ]
        },
        "properties": {
          "address": "the record's address at the channel: the slug a route names, freely editable until the record was ever published there and a permanent citation target afterwards",
          "channel": "the channel; one binding per record and channel is the extension's rule, checked by a read before the write",
          "disclosure": "the synthetic-media disclosure the channel's control needs",
          "fields": "the values of the container's fields the site declares, typed as the index declares them",
          "id": "the binding's identity, a UUID the extension mints; the node id is node:<id>",
          "number": "the record's number at the channel, for a numbered container; typed by the author",
          "recordId": "the deliverable or item this binding is about",
          "recordKind": "what kind of record"
        },
        "requiredProperties": [
          "id",
          "recordId",
          "recordKind",
          "channel",
          "fields"
        ],
        "semantics": "What one record is worth at one channel, as authored. Observed facts — when it was published, its state — are never stored here; they are the log's. PU_0003_001",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:channel",
      "type": "ext.blocktype",
      "content": {
        "id": "channel",
        "name": "channel",
        "properties": {
          "id": "the channel's identity, a UUID the extension mints; the node id is node:<id>",
          "kind": "the id of the channel kind this channel is of — an adapter the publishing extension ships; the registry in code is the roster, so the value is unconstrained here",
          "title": "the channel's title, the author's own words for where this goes"
        },
        "requiredProperties": [
          "id",
          "kind",
          "title"
        ],
        "semantics": "A destination the author's work goes to, created by the author from the Channels category: a kind and a title. Its credential and configuration are a party record in the kernel's secret store under publishing-<id>, never in the graph, and its state is read from that record. It declares a contentIndex through `declares` when its kind reads an index from the destination. PU_0001_002",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:contentIndex",
      "type": "ext.blocktype",
      "content": {
        "id": "contentIndex",
        "name": "contentIndex",
        "optionalProperties": [
          "containers",
          "slots",
          "copy"
        ],
        "properties": {
          "channel": "the channel that declared it; one index per channel is the extension's rule, checked by a read before the write",
          "containers": "the containers the site declared, each with whether the last read still listed it",
          "copy": "the site-wide copy fields the site declared, each with whether the last read still listed it",
          "id": "the index's identity, a UUID the extension mints; the node id is node:<id>",
          "readAt": "when the site was last read, as an ISO-8601 timestamp the extension stamps at the write",
          "slots": "the slots the site declared, each with whether the last read still listed it"
        },
        "requiredProperties": [
          "id",
          "channel",
          "readAt"
        ],
        "semantics": "What a website declared at GET <address>/index, as the publishing extension stored it: containers with route templates and fields, slots typed by content class with their constraints, copy fields. A key the site no longer lists is kept and marked while something is assigned to it, dropped otherwise. PU_0001_002 PU_0001_007",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:deliverable",
      "type": "ext.blocktype",
      "content": {
        "id": "deliverable",
        "name": "deliverable",
        "properties": {
          "id": "the deliverable's identity, a UUID the extension mints; the node id is node:<id>",
          "title": "the deliverable's title"
        },
        "requiredProperties": [
          "id",
          "title"
        ],
        "semantics": "An instance of a shape (`shaped`), gathering the items and nested deliverables that fill its parts (`gathers`). Nothing on it says where it went; that is the publication log's. PU_0002_001",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:export",
      "type": "ext.blocktype",
      "content": {
        "id": "export",
        "name": "export",
        "optionalProperties": [
          "width",
          "height"
        ],
        "permittedValues": {
          "provenance": [
            "ingested",
            "produced"
          ]
        },
        "properties": {
          "bytes": "the blob reference: _kind blob, hash sha256:<objectId>, mediaType, size and the filename it arrived as",
          "height": "the export's height in pixels",
          "id": "the export's identity, a UUID the extension mints; the node id is node:<id>",
          "mediaType": "the media type read off the file, restated beside the reference",
          "provenance": "how it came to exist: ingested from outside, or produced here",
          "size": "the size in bytes, restated beside the reference",
          "width": "the export's width in pixels; its aspect is derived from the dimensions on the way out"
        },
        "requiredProperties": [
          "id",
          "bytes",
          "mediaType",
          "size",
          "provenance"
        ],
        "semantics": "One rendition of an item: content-addressed bytes in the graph's blob store, permanent once referenced. Replacing an export closes the item's `exported` relation to the old one and keeps it. PU_0002_001",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:item",
      "type": "ext.blocktype",
      "content": {
        "id": "item",
        "name": "item",
        "optionalProperties": [
          "label",
          "durationSeconds",
          "width",
          "height",
          "transcript",
          "alt"
        ],
        "permittedValues": {
          "class": [
            "video",
            "image",
            "audio",
            "prose",
            "file"
          ]
        },
        "properties": {
          "alt": "the alt text of a picture",
          "class": "the content class",
          "durationSeconds": "a video's or an audio's duration, read off the file",
          "height": "a picture's height in pixels, read off the file",
          "id": "the item's identity, a UUID the extension mints; the node id is node:<id>",
          "label": "a short line naming the item, the author's",
          "synthetic": "whether the item is synthetic media; stated per item, never defaulted, so a destination applies its own disclosure from it",
          "transcript": "the words a video or an audio says, so an agent that cannot watch can read",
          "width": "a picture's width in pixels, read off the file"
        },
        "requiredProperties": [
          "id",
          "class",
          "synthetic"
        ],
        "semantics": "One piece of content: its class, its machine facts, a label and its disclosure; its exports through `exported`, or its body document through `prose`. It may be gathered by several deliverables or by none; one gathered by none is standing. PU_0002_001",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:part",
      "type": "ext.blocktype",
      "content": {
        "id": "part",
        "name": "part",
        "optionalProperties": [
          "role",
          "constraints",
          "shape"
        ],
        "permittedValues": {
          "cardinality": [
            "one",
            "optional",
            "some",
            "any"
          ],
          "class": [
            "video",
            "image",
            "audio",
            "prose",
            "file",
            "shape"
          ]
        },
        "properties": {
          "cardinality": "how many fill it: exactly one, at most one, one or more, any number",
          "class": "the content class the items filling it are of, or `shape` for a part that nests another shape",
          "constraints": "what the author requires of the items filling it at production time: aspect, maxDurationSeconds, minWidth, minHeight, formats",
          "id": "the part's identity, a UUID the extension mints; the node id is node:<id>",
          "order": "the part's place among its shape's parts: a fractional key compared as a string",
          "role": "a label for what the author makes the part as",
          "shape": "the shape a part of class shape nests",
          "title": "the part's title"
        },
        "requiredProperties": [
          "id",
          "title",
          "class",
          "cardinality",
          "order"
        ],
        "semantics": "One part of a shape: a content class with a cardinality, a role and constraints, or a nested shape. Items fill it through `fills`. PU_0002_001",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:release",
      "type": "ext.blocktype",
      "content": {
        "id": "release",
        "name": "release",
        "optionalProperties": [
          "recordTitle",
          "externalId",
          "externalAddress",
          "detail"
        ],
        "permittedValues": {
          "act": [
            "publish",
            "retire"
          ],
          "outcome": [
            "succeeded",
            "failed"
          ],
          "recordKind": [
            "deliverable",
            "item"
          ]
        },
        "properties": {
          "act": "publishing puts what the channel did not hold; retiring ends what it did",
          "at": "when, as an ISO-8601 timestamp the extension stamps at the write",
          "channel": "the channel",
          "detail": "what went wrong, on a failure; never carries a presented secret",
          "externalAddress": "where the channel serves it, on a success",
          "externalId": "the channel's own id for what it now holds, on a success",
          "id": "the entry's identity, a UUID the extension mints; the node id is node:<id>",
          "objectIds": "every content-addressed object the attempt sent, which are permanent once named here",
          "outcome": "succeeded or failed",
          "recordId": "the record the attempt was about",
          "recordKind": "what kind of record",
          "recordTitle": "the record's title at the time, so the log reads without the record"
        },
        "requiredProperties": [
          "id",
          "recordId",
          "recordKind",
          "channel",
          "act",
          "outcome",
          "objectIds",
          "at"
        ],
        "semantics": "One entry of the append-only publication log: created as an established revision and never revised, retired or removed. A record's state at a channel is derived from its entries. PU_0003_001",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:shape",
      "type": "ext.blocktype",
      "content": {
        "id": "shape",
        "name": "shape",
        "properties": {
          "id": "the shape's identity, a UUID the extension mints; the node id is node:<id>",
          "title": "the shape's title, the author's word for the kind of deliverable it describes"
        },
        "requiredProperties": [
          "id",
          "title"
        ],
        "semantics": "A deliverable type the author defines — Episode, Statement, Weekly edition — composed of parts through `composes`, and nesting other shapes through `nests`. Which shapes exist is data; the concept is the publishing extension's. PU_0002_001",
        "singleOpenCandidate": true
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:composes",
      "type": "ext.relationtype",
      "content": {
        "id": "composes",
        "name": "composes",
        "semantics": "A part of a shape. Directed shape to part."
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:declares",
      "type": "ext.relationtype",
      "content": {
        "id": "declares",
        "name": "declares",
        "semantics": "The index a channel declared. Directed channel to contentIndex; at most one active per channel."
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:exported",
      "type": "ext.relationtype",
      "content": {
        "id": "exported",
        "name": "exported",
        "semantics": "An export an item still exports. Directed item to export; closed when the export is replaced, the export kept."
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:fills",
      "type": "ext.relationtype",
      "content": {
        "id": "fills",
        "name": "fills",
        "semantics": "The part an item or a nested deliverable fills. Directed item or deliverable to part."
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:gathers",
      "type": "ext.relationtype",
      "content": {
        "id": "gathers",
        "name": "gathers",
        "semantics": "What a deliverable holds: an item, or a deliverable of a nested shape. Directed deliverable to item or deliverable; an item may be gathered by any number of deliverables."
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:nests",
      "type": "ext.relationtype",
      "content": {
        "id": "nests",
        "name": "nests",
        "semantics": "A shape a part of another shape nests. Directed outer shape to inner shape; never a cycle."
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:of",
      "type": "ext.relationtype",
      "content": {
        "id": "of",
        "name": "of",
        "semantics": "The shape an assignment takes. Directed assignment to shape."
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:prose",
      "type": "ext.relationtype",
      "content": {
        "id": "prose",
        "name": "prose",
        "semantics": "The body document of a prose item, in the shell's block model. Directed item to document; at most one active per item."
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:shaped",
      "type": "ext.relationtype",
      "content": {
        "id": "shaped",
        "name": "shaped",
        "semantics": "The shape a deliverable is an instance of. Directed deliverable to shape; exactly one active per deliverable."
      }
    },
    {
      "extension": "publishing",
      "nodeId": "node:takes",
      "type": "ext.relationtype",
      "content": {
        "id": "takes",
        "name": "takes",
        "semantics": "An assignment of a channel. Directed channel to assignment."
      }
    },
    {
      "extension": "ui.shell",
      "nodeId": "node:attachment",
      "type": "ext.blocktype",
      "content": {
        "id": "attachment",
        "name": "attachment",
        "optionalProperties": [
          "text"
        ],
        "permittedChildTypes": [],
        "permittedValues": {
          "textStatus": [
            "text",
            "extracted",
            "empty",
            "image",
            "none"
          ]
        },
        "properties": {
          "file": "a blob reference to the file's bytes; writing the node references them, so the blob store keeps them past the staging TTL",
          "filename": "the file's name as the person attached it",
          "id": "the attachment's identity, a UUID the shell mints when the command is sent; the node id is node:<id>",
          "mediaType": "the file's media type: the browser's when it named one, else sniffed from its first bytes, else its extension's",
          "size": "the file's size in bytes, at most 10 MB",
          "text": "a blob reference to the text extracted from a PDF or Office document; absent for a file that is its own text and for any file with none",
          "textStatus": "what a run can read of the file: text, extracted, empty (a PDF with no text layer or an Office file with no text), image, or none"
        },
        "requiredProperties": [
          "id",
          "filename",
          "mediaType",
          "size",
          "file",
          "textStatus"
        ],
        "semantics": "The record of a file a person sent with a command: written as that person when the command is sent, one node per file, and named by the run it was sent with. It is the frame's, not a document's: nothing contains it and it contains nothing. Declared by ui.shell under BO_0229_007."
      }
    }
  ],
  "extensions": [
    "calliopa-base",
    "calliopa-extension",
    "documents",
    "publishing",
    "settings",
    "ui.shell"
  ]
}
