# External API

## External API

- This topic is a link since `BO_0207_016`. What it described — the shell's own external JSON API and MCP tool surface — retired with the shell's stores; the surface automation reaches the graph through is the core's now.
- Automation and agents reach the one graph through the core: CCGW's statement routes and the kernel toolset's statement and document tools (`ui-kernel.md`, `BO_0207_004`), under credentials the core issues (`BO_0206`). The shell serves no JSON API or MCP surface of its own (`BO_0207_015`).
- The shell keeps no database, no bucket key and no gateway of its own (`ui-shell.md`, `BO_0207`); the history of the retired implementation stays in this topic's earlier revisions.
