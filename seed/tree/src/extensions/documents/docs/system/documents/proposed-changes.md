# Proposed Changes

## Proposed Changes To A Document

* A proposal group is staged against one document. A caller touching three documents stages three groups, because a group is against one document and cross-document atomicity is a promise nothing has asked for.
* An item is one of `replace`, `insert`, `remove`, or `move`, and each carries everything its operation needs.

- `replace` names a block and carries its proposed runs and role, staged as a candidate revision of that block. Accepting it establishes that revision, which is the same supersede an ordinary save performs.
- `insert` carries a new block's type, its content, and the position it takes as a sibling and a side, the shape `insertBlock` already accepts. Accepting it creates the block and the containment that places it as one item, which is why it cannot be half-accepted into a block no document contains.
- `remove` names a block and retires it on acceptance, closing containment and creating the `retired` relation in the same mutation, as retiring already does. It is recoverable from the retired blocks like every other retirement.
- `move` names a block and the position it would take. The key lives on the block, so a move is a revision of its content like any other and stages a candidate; accepting establishes it and containment is untouched.
- The order key is minted as an item is staged, against the document as it then stands, and each insert of a group counts the ones staged before it as siblings so two appends do not mint the same key. Minting at acceptance was the alternative and would have made the item's own content depend on when it was answered; a fractional key always sorts somewhere, so an item accepted after the document moved still lands where it said it would rather than nowhere.
- A stale item is one naming a block the document no longer contains. It is refused rather than applied to something else, because the block it was staged against is gone and a document that removed it is a conflict rather than a target.
- Containment validity holds after every acceptance order. Exactly one active containment parent per block is the invariant, and accepting an `insert` and a `move` against the same parent in either order leaves a valid document.
- Staging does not change the document and does not count as a change to it. A candidate revision is not part of the document until it is established, and a count that moved while nothing the reader can see moved would report a change they cannot find. Acceptance is what counts, as the write it performs.
- The ordered document read is unaffected by open items. It follows established truth, so a document with a group standing against it reads exactly as it did before the group was staged.

- `proposeDocumentChanges` in `src/server/documents/documents.ts` compiles the four kinds into one proposal-scoped mutation script and stages it through the kernel bridge's `stage` verb into a fresh CCGW group the shell names (`node:chg-<hex>`): a `replace` is a content `SET` on the block and lands as its candidate revision, a `move` the same on its order key, an `insert` a `CREATE` of the new block with the `RELATE` that places it — a relation anchored at the member's own candidate, which travels with that member at acceptance — and a `remove` a `CLOSE` of the containment, recorded as the group's close intent, with the `RELATE` of the `retired` relation. An item naming a block the document does not hold is refused as it is compiled rather than reaching the graph (`CA_0023_010`, `BO_0207_012`).
- `readDocumentProposals` derives the document's items from each open group's touched set and candidates, since a CCGW group names no document: a staged `CONTAINS` from the document to a node with no established truth is an `insert`, a staged `retired` from the document is a `remove`, and a candidate of a contained block that differs from its established content is a `move` when only the order key differs and a `replace` otherwise; a candidate equal to established content is the carry-forward anchor a staged relation hangs on and is not shown. A proposed block goes through `toBlock`, the same reader the document's own blocks go through, so a proposed block and a stored one cannot come to mean different things (`CA_0023_010`, `BO_0207_012`).
- An item's identity is the group, the kind and the members its decision covers, joined by `|`: one member for a replace, a move or an insert — the staged node — and two for a remove — the staged `retired` relation and the containment the close intent names, decided in that order so a block is never detached before it is recorded as retired. `answerDocumentProposal` issues one bridge `accept` or `reject` per member; a content member executes without a confirmation (`ui-kernel.md`, `BO_0207_001`), a member the kernel keeps behind its confirmation answers as refused rather than half-done, and CCGW's drift judgement on a rewritten block answers as a conflict against the item, with `override` the explicit accept-over. The group's state after the answer is read back so the caller learns it closed without asking (`CA_0023_010`, `BO_0207_012`).
- `GET /api/x/ui.shell/documents/[id]/proposals` reads them, and `propose` and `answerProposal` are commands on the existing command route. Proposing is a command like any other because an in-application proposer — an import, a bulk transform — reaches a document through the same transport its editor does (`CA_0023_011`). An agent proposes through the kernel toolset's document tools once `BO_0207_004` lands, staging into the run's own group.
- `tests/behavior/documents.test.ts` proves the lifecycle over the one graph: a staged group leaving the document and every revision exactly as they were, the items read back against their blocks with the proposed content, an insert accepted establishing the block with the containment that travels with it while the group stays open, and the replace rejected leaving the block as it was and closing the group (`BO_0207_012`). Before it, `tests/integration/block-documents.test.ts` and `tests/integration/document-api.test.ts` proved the shell's own lifecycle against real Postgres (`CA_0023_010`, `CA_0023_011`).

## Proposed Changes In Place

* The panel carries a `Show proposed changes` toggle. Turning it on shows the document's unanswered proposal items in place, each against the block it concerns.
* A proposed item is visibly not truth, visibly not the active block, and visibly not a retired block, and carries an accessible name saying what it proposes and by whom.
* A proposed item says who proposed it by a ground and edge of the proposer's own, and its accessible name says who proposed it and what it does. When the reader turns to it — hovering it or focusing it on a desktop, a first tap on a phone — one chip on its bottom border shows the proposer's face, what the proposal does in words, and its answers, the face's name saying the same in words (`BO_0233`; the line since `BO_0265`; the chip, hidden at rest, since `DO_0004`, user decisions 2026-09-18).
* An item is answered where it is read, by icons on that line after its words: accept and reject. A run's items are answered together from its run chip above the command field, by *Reject all* and *Accept all* ([The Agent At Work](./agent-at-work.md)). User decision, 2026-09-18.
* Editing a proposed item's text accepts it: once the reader's typing pauses, or they leave the text, the item is established and what they typed is made on top as the reader's own revision (`BO_0233`).
* Editing a proposed rewrite accepts it even when its block's words, role or place changed after it was staged, and makes what the reader typed on top: the reader's edit to the block since is replaced by the proposal's text and the typing, and the reader's standing stays theirs. Accepting such a rewrite from its ✓ or by *Accept all* is still refused in words, and so is a move, which takes no typing. User decision, 2026-09-11 (`CA_0042_001`).
* Moving a proposed item does not answer it: its new place is staged into its own group, and accepting it later lands it there (`BO_0233`).
* Accepting an item changes the document under the reader at once, because an accepted item is truth.

- The panel states how many items stand unanswered whether or not the toggle is on. A toggle that hid the fact that work was waiting would leave a reader with no way to learn it, and the count is a fact about the document like the change count beside it.
- The toggle is off by default and its state is the tab's, following the retired-blocks toggle exactly and for its reason: what it reveals is not the reading order, and the resting surface stays what the document says.
- A `replace` shows its proposed text beside the block's current text, because judging a rewrite means seeing it against what it replaces.
- An `insert` shows as a proposed block at the position it would take. A `remove` marks the block it would retire, which stays readable and editable meanwhile. A `move` marks the block where it now stands and says where accepting would put it, rather than drawing the block twice and asking the reader which one is real.
- The proposed treatment is distinct from the retired treatment and from the marked treatment [Command Mode](./command-mode.md#command-mode) uses, by more than colour, and it leaves no trace once nothing is proposed. Three treatments can share a surface only while a reader can tell at a glance which is which.
- Both toggles may be on at once. Retired blocks and proposed items interleave in the same reading order, each keeping its own treatment.
- A proposed item is markable in command mode, by its face and, for a rewrite and a new block, by its words, and the mark points at the proposal rather than at what the document says ([Marking Every Drawn Row](./command-mode.md#marking-every-drawn-row), `BO_0263`). Its words take no typing in command mode. Since `BO_0233` a rewrite's and a new block's text take the caret while reading, and a change accepts them — the user's decision of 2026-09-10, that editing a proposal is answering it.
- While the reader types into a proposal nothing is accepted and nothing is handed over: the words show as typed, and the proposal keeps its face, its colour and the caret. Accepting turns the proposal into the block, with the block's look; when the first keystroke accepted, that happened under a typing hand, and the caret and the keystrokes typed meanwhile were lost (`BO_0233_014`). So the moment is the block editor's own save pause, or leaving the text: a reader still in it is given the block, activated where their caret is, before the proposal goes, and a reader who has left has what they typed written to the block without the caret being taken back. A new paragraph or a format is the block's to carry out, so it hands over at once. Answering a proposal from its icons while its typing waits keeps the typing if it accepts, and discards it if it rejects.
- A conflicted item says what moved and stays answerable, which is the per-item conflict [Graph Gateway](../content-store/proposals.md) reports. A stale item says its block is gone and offers `Reject` alone, so a group holding one can still close.
- CCGW judges a member's drift by revision: any revision of the block established after the group's base conflicts, including one that set only its standing. Accepting a rewrite or a move whose block moved only in its standing — its words, role and order at the base are what truth holds now — is done over the drift, and the reader's standing is written back afterwards, since a standing says nothing about the words. One whose words, role or place changed is refused in words (*made against an older version of the block … Reject it, or ask for it again*) rather than with the edit conflict's *reload*, which would change nothing — unless the reader typed into the rewrite, which accepts it over the drift (`CA_0042_002`). Found on `BO_0233`'s walk-through, when a pinned block's proposal could not be accepted (`BO_0233_011`).
- Answering an item is a saved operation and the undo keystroke does not reverse it, as it reverses no other saved structural operation. Rejecting is how a reader declines a change, and a rejected item is recoverable only as the record of what was asked for.
- Review happens in the document rather than in the inspector: [Workspace Shell](../workspace/layout.md) fixes what the shell renders from a view's contribution — a save state, a time, a count, a line of text, a button, and a toggle — and none of that expresses proposed text against current. Growing that vocabulary for one consumer would put a document's text in a drawer beside the document.
- A review tab of its own was rejected for the same reason: it judges a block's text away from the document that block lives in.
- A reader stages only a proposal's place. Until `BO_0233` the rule was *nothing here stages a proposal*; dragging a proposal, or moving it by the arrow keys, now stages its new order key into the group that holds it — a group an agent may have opened, which CCGW admits — and nothing else. The editor still answers what another caller staged, and a human editing a document still writes truth directly.
* Proposed relations and claims are reviewed in place (`BO_0244`, the second intent line: inferred is proposed). A run stages five further item kinds beside the four — `kind`, `claim`, `relate`, `reason`, `derive` — and each is answered where it is read, by the icons every item has; editing a relation's reason accepts it with the edit on top, as editing a rewrite does (`BO_0233`, `CA_0042`).
- A system run's derived candidates are drawn in the derived idiom, not as an agent's proposal (`BO_0246_006`): `readDocumentProposals` marks a group's `insert` and `replace` items `derived` when the group's `agent.run` node carries `trigger: "system"` — read already for the proposer — and the item's block kind is one of `DERIVED_KINDS` (`synthesis`, `frontier`, `tension`, `alternative`, `consequence`, `next`; `documents.ts`). The editor places them under the derived headings of `CA_0046` (`reading-order.ts`: a derived insert lands in its section after its last row, or under the heading it opens), shows them with the *Show proposed changes* toggle off, in the body's quiet idiom with the derived mark on focus (`ProposalBlock` `derived`; `proposal-block.tsx`), with the answer icons and, beside them, *Fixate this framing* and *Discard* (`data-derived-fixate`, `data-derived-discard`). They are answered explicitly since `BO_0258_014`: the icons, or *Fixate this framing*, which accepts through `answerDocumentProposal` and then stands the block the acceptance established (`useDerived$`, `settleDerived$` in `block-editor.tsx`), or *Discard*, which rejects it without a standing; a later system run that replaces one rejects the old candidate as it stages the new. An established derived block's rewrite is drawn beside it in the derived idiom as a proposed rewrite is, and a fixated block's rewrite says *challenges the fixated framing* in its accessible name (`derivedNames`, `lib/proposals.ts`); a run never overwrites a fixated block (`BO_0246`, Decided; the reader's own swipe does, `BO_0272`). Referencing accepts a derived rewrite of the referenced block; an inserted candidate has no row to reference until it is fixated or edited into. What a system run stages that is not a derived block — the `question` beside a fixated framing it disagrees with — keeps its answer icons but is attributed to the system, not to an agent: the `Proposer` reads `kind: "system"` from the run's `trigger` (`proposerOf`, `src/lib/proposals.ts`), named *the system, as Claude Code*, with the sparkle as its face and the panel's own ground rather than an agent's colour (`data-proposal-tone="system"`). Found in the walk-through, 2026-09-14: a refinement's question read as *a proposal from Claude*.
- A candidate `relation` with `origin: inferred` renders under its source block as the *Possible relation* card (`BO_0247_005`; `PossibleRelation` in `proposal-block.tsx`, `data-relation-card`, chosen by `isInferredRelation`): the derived idiom, an *Inferred* chip, the claim it anchors on, the kind, the target quoted with its document's title, the lines *Origin: system-inferred* and *Reason: system-drafted*, and the reason itself editable in place (`ProposalReason`, so leaving an edit accepts with the edit on top as editing a rewrite does, `BO_0233`). The card carries no buttons of its own: it is answered from the chip on its bottom border like every other proposal (`DO_0007_001`, *The Relation Card Answers In Its Chip*). A person's declared relation keeps the `relate` work item of `BO_0244_010` with its icons. The card is drawn on the source block in the source's document; a cross-document relation appears once, on its source — `readWorkItems` marks one whose source is elsewhere and the reading order skips it while `unanswered` counts it. Cards in quantity: under one block the first two draw in full and the rest collapse to one line, *and N more possible relations* (`data-relations-more`), that opens them for the page — decided at transfer, 2026-09-14, for the walk to confirm or revise once a real refinement shows how many arrive.
- Verified 2026-09-14 on the tree (`BO_0247_009`): `tsc --noEmit` clean, the unit project 69 files and 668 tests — `src/components/views/proposals/relation-card.test.ts` presses the card and its parts, a person's declared relation keeping its icons, confirm and dismiss answering, an edit accepting with the edit on top, the collapse after two, and the provenance line in the layer; `src/lib/depth-provenance.test.ts` the words — both bundles built; `tests/behavior/relations-provenance.test.ts` under the kernel harness: a declared relation reading declared, drafted by the person, no confirmer; an inferred one confirmed reading system-drafted and confirmed by the person, then later edited by them; a dismissed one leaving nothing.
- Walked on the served build on 2026-09-14, from pin 579 to pin 618, and the user set the change completed (`BO_0247_009`): a real refinement's *Possible relation* card under the block whose sentence the run related to the document's definition, with its chip, claim, kind, target, origin and reason lines; the relation confirmed with the reason edited on top; and the provenance line under the relation in the definition's *Evidence* layer reading *system-inferred · system-drafted reason · confirmed by frankzickert · later edited by frankzickert*. Two things changed on the way, staged and accepted: the shell's provenance read orders a node's revisions by the revision each was created at, since the history read had put the archived drafter revision in the current slot and swapped drafter and editor; and the `refine.relate` skill reads the other documents themselves, with the kernel listing them by title and opening words in the context, after two runs judged a copied block unrelated on its title alone. The collapse after two cards was not exercised, since one relation arrived.
- Only a group's candidates are items. A member keeps the group's `_proposal` stamp once accepted and established, so `readDocumentProposals` reads every stamped node for the stager and the run but builds items from the candidate revisions alone; an established member shown again would offer an acceptance the core refuses as *not an undecided member* (found live in the `BO_0248` walk-through, 2026-09-14, on a confirmed relation card after a reload; `work.test.ts` reads the group again after accepting).
- The `phase` item (`BO_0249_011`): `proposeDocumentChanges` takes `{kind: "phase", phase, supersededBy?}` and stages `SET d.phase = $phase[, d.supersededBy = $by]` on the document node, refusing `notARoot` on a change document, `unknownPhase`, and `supersededByWithoutSuperseded`; the staged item is `{itemId: <group>|phase|node:<document>, kind: "phase", blockId: ""}`, and the kernel's item of the same name stages the same candidate for a run (`ui-kernel.md` `BO_0249_001`). `readDocumentProposals` reads a group's candidate of the document node whose `phase` differs from the established one as a `phase` item — `{kind: "phase", blockId: "", block: null, phase, supersededBy?, sentence?}`, `sentence` the group's `rationale` (the run's sentence of consequences) — which the editor draws as the transition card under the intent (`document-panel.md` `BO_0249_009`), never as a proposal block; *Establish* accepts it through `answerProposal` with the document node as the one member, *Not yet* rejects it. The candidate is a real member, not a carry-forward, since its content differs in `phase`; the document's single open-candidate rule counts members only, so the phase stages beside the groups whose inserts anchor at the document and a later insert stages beside it (`ccgw.md`, found live 2026-09-15 when a run's phase item was refused on a document with open derived inserts).
- A branch's items for others and promotion (`BO_0250_016`): `readDocumentProposals` lists a branch as a group whose proposer is the person its name carries — *Proposal · <name>* — drawn in the person's colour with its items answered per item as any group's when the reader is not its person; for its person, in the branch, the items are the document and not proposals (the client hides the tab's own branch). *Promote this block on its own* (`document-panel.md` `BO_0250_022`) is the `promoteBlock {group, blockId}` command: `promoteBlock` in `documents.ts` reads the block's candidate through the rejected group's overlay, outside the tab's branch, and stages it into a new group of the person's, `node:promote-<block>-<account>-<n>` with `n` one past the groups so named, as an insert at the document's end when the block is not in truth and a rewrite of its runs and role when it is, answering the group and the item; a block that is not a member of the group is refused `notInBranch`.
- No surface marks a branch member settled (`BO_0250_013`, `BO_0250_023`, user decision 2026-09-16): the proposals read carries no mark, the commands route has no settle command, and no block draws one. The core's `SETTLE`/`UNSETTLE` mark and the kernel's settle verbs stay (`ccgw.md` `BO_0250_001`), used by no surface.
- A carry-forward anchor is never an item: a group's candidate that copies a block's established revision so a staged relation can anchor at it — the block a derived block's `derivedFrom` or a claim's `asserts` edge reaches — is named by the core's touched set (`carryForwardNodes`) and skipped by `readDocumentProposals`, since once truth moves past the copy it would read as a rewrite back to the older words and acceptance refuses it as *not a decidable member* (found live in the `BO_0248` walk-through, 2026-09-14).
- A `state` item (`ui-kernel.md` `BO_0248_004`) is reviewed in place (`BO_0248_013`): `readWorkItems` emits `{kind: "state", relationId, state, previousState, reason}` when a staged relation's state differs from the established one, placed by the relation's source block, and the editor draws it as a proposal row under that block — *Proposed state: set to <state> (now <previous>)* with the relation and the reason — in the proposer's idiom with the answer icons; accepting writes the state, rejecting leaves it.
- `readDocumentProposals` derives the work items from a group's touched set and candidates (`readWorkItems`, `BO_0244_010`): a candidate `text` block that differs from truth only in its kind is a `kind` item; a candidate `claim` a staged `asserts` from one of this document's blocks points at is a `claim` item, and a candidate of a claim the document asserts whose words differ is one too; a candidate `relation` node whose `source` or `target` is a claim here is a `relate` item — the claim the same group drafts for it folds into its members, decided first so the relation is never established with an end still a candidate — and one that already has established truth is a `reason` item; staged `derivedFrom` edges from a block here are a `derive` item whose members are the edges, unless the group also revises that block, in which case the edges travel with the block's own candidate and the derivation is said on that item (`derivedFrom`) rather than answered on its own — CCGW decides a relation with the member it anchors at. `proposeDocumentChanges` compiles the same five (`compileRelate` and the item cases), a relation's end resolved as the operations resolve one, and names a `derive` item by its staged edges after staging, from the touched set.
- A work item is drawn under the block it concerns (`ProposalWork` in `proposal-block.tsx`), in the proposer's ground with no place of its own — no drag, no arrow keys. A relation is *Possible relation* with its origin (*Inferred*, *Derived*, *Declared*): the source claim quoted, what it does to the target claim, quoted with its document when it is another's, and *Reason*, editable where it is read (`ProposalReason`); a proposed reason on a standing relation is *Proposed reason* the same way. Once the typing pauses or the reader leaves the reason, the item is accepted as an edit's acceptance and the relation's reason revised to the typed words from the revision the acceptance established (`settleReason$` in `block-editor.tsx`, the `reviseReason` command); a reason left as it was accepts nothing, and a refused acceptance puts the proposal's words back. A claim is drawn as its words under *Proposed claim*, a kind as *Proposed kind: …*, and an item that carries a derivation says *Derived from n blocks*; neither claim nor kind takes the caret. A relation whose source is in another document is drawn there: this document counts it among its unanswered items and draws nothing (`elsewhere`, `placeProposals`). A declared relation is never drawn on the resting surface; it is the block's depth's to show (`CA_0046`). `proposal-block.test.ts` presses the cards in the render harness: the relation under its block with claim, kind, target, document and origin, its reason editable, no face to drag; the claim and the kind as words with the derivation; the elsewhere item counted and undrawn; the reason edited and left accepting as an edit and revising on top; the accept icon answering never as an edit.
- A document's proposals read reads only the groups that reach the document (`BO_0257_008`, `calliopa-bootstrap`'s `BO_0257`): the open groups are listed, and their touched sets are read all at once — one cheap core read each — before any group's members are; a group is read further only when a node it stages or intends to archive, or an end of a relation it stages or closes, is the document's node, one of its blocks, a claim they assert, or a relation node on those claims (`reachesDocument`, `server/reach.ts`). That set is every node an item is drawn from — a new relation between two claims here reaches through its staged `source` or `target`, a revised reason through its relation node — so the items are unchanged. Reading every open group's members made a document open wait on every proposal in the instance: 4.3–5.2 s for `Walkthrough` with 47 open groups on 2026-09-16, 0.7 s after the core's read fixes. Verified by `server/reach.test.ts` and the behavior project under the kernel harness, the refine suites' proposals, relations, branches and phases included.
- The groups that reach the document are read all at once (`BO_0257_010`): each group's members, work items and marks are one `readGroup` of its own, run together and answered in the listing's order, so a document many standing refinements reach waits for the slowest group rather than for all of them in turn. Measured at pin 1182 on 2026-09-16: `Walkthrough`, reached by 18 of 48 open groups, read in 0.49–0.77 s one group after another.

## Accepting Keeps The Place

- Under `DO_0004`, set to draft by the user on 2026-09-18 and transferred here the same day. The user saw an accepted proposal land somewhere other than where it was drawn, perhaps after dragging it.
* Accepting a proposal leaves its block where the reader sees it. User decision, 2026-09-18.
- Two ways the drawn place and the place an answer writes can part, neither confirmed: a shown rewrite is drawn in its block's place (`CA_0055_005`, `placeProposals`), so a dragged rewrite's staged key (`BO_0233_007`) can point elsewhere while the row stays put; and an arrow step is staged only after `PROPOSAL_STEP_PAUSE_MS`, so an answer pressed inside that pause answers the place staged before.
- An accepted proposal lands where it is drawn (`DO_0004_008`). A rewrite the reader dragged or stepped had its new key staged into its candidate (`BO_0233_007`) but stayed drawn in its block's place (`CA_0055_005`), so accepting it moved the block to a place the reader no longer saw. `placeProposals` (`views/reading-order.ts`) now draws a rewrite whose staged key differs from its block's where that key puts it, as a new block, still standing for its block (`replaces`), whose row is not drawn; `keyOf` reads the rewrite's own key. An arrow step waiting out `PROPOSAL_STEP_PAUSE_MS` is held as `proposalEdit.pending`, and `flushProposalStep$` stages it before `answerProposal$` or an edit's `settleProposal$` answers that proposal, so an answer pressed inside the pause answers the place the reader sees; the timer then stages nothing twice. A proposed move still lands where its sign says it would go: that is what it proposes. `views/proposals/chip-and-press.test.ts` proves a moved rewrite drawn at its key with its block's row gone, and a rewrite stepped down and accepted at once drawn where it went, with its place staged once and before the answer; each test failed with its fix taken out. The walk at pin 367 found a second way (2026-09-18): the agent run's inserts on the `Test` document shared order keys — three blocks at `d`, three at `f` — since each insert of a group was minted against a sibling list the staged ones were appended to unsorted, so every insert "after" one block took the key between that block and the next. Blocks sharing a key read in block-identity order, while a drawn insert stood before every established block of its key, so accepting one moved it and its neighbours. `placeProposals` now breaks a tie by block as `readingOrder` does, so a proposal beside blocks sharing its key is drawn where accepting puts it. `orderFor` (`server/documents.ts`) now mints against its siblings in reading order, passes the blocks the group already put after an anchor or at the start, and mints against the nearest neighbour whose key differs from the anchor's, so the inserts of one `propose` keep their order under distinct keys and a shared key never sends one to the end. `server/order.test.ts` and `views/reading-order.test.ts` prove both and failed without them. The kernel's agent tools mint a run's keys by their own copy of the rule (`internal/kernel/agenttools/documents.go`, `placementKey`) and have the same flaw; that is the fixed layer's to fix.

## Implementation

- The panel contributes the count of unanswered proposed changes as a fact, and the bar `Show proposed changes` as a toggle beside the retired one (`CA_0053`). The proposals are read whenever the document is, not only when the toggle is turned on, because the count is what tells a reader work is waiting while the items are hidden (`CA_0023_012`).
- `placeProposals` puts each item where it belongs: an item that would insert a block carries an order key and sorts among the blocks by the comparison siblings already sort by, and an item naming a block sits immediately after that block, because judging a rewrite means seeing it against what it would replace. An item whose block has gone since is drawn at the end rather than dropped, so a group the reader must answer never hides an item they cannot find. It is pure, so where an item lands is settled without a document (`CA_0023_012`).
- Until `BO_0233`, `.proposal-row` was solid where the retired row is dashed, took the accent edge where that one takes a muted one, and carried a standing label naming what was proposed and who staged it — *Proposed rewrite · staged by agent:hermes*. The label named the account every agent stages under (`BO_0206_008`), so a Claude run's rewrite read as Hermes's, and it was removed by the user's decision of 2026-09-10 (`CA_0023_012`).
- A proposed item is drawn as the block it would become (`ProposalBlock`, `src/components/views/proposals/proposal-block.tsx`): `.block-row`'s gutters, its role's element and typography, in its proposer's ground and edge. The proposer is read from the group's own `agent.run` provenance node — its `agent` and `executedBy` — and a group a person staged is the person's; an agent-stamped group with no provenance node says *an agent* (`readDocumentProposals`, `proposerOf` in `src/lib/proposals.ts`). The chip on the bottom border (`.proposal-block__mark`) holds the face, which is the handle, the words (`.proposal-block__words`) and the answers — Phosphor `check` and `x` — every control 24px ([The Agent At Work](./agent-at-work.md#the-proposal-chip), `BO_0265_010`, `DO_0004_003`). A shown rewrite stands in the place of the block it rewrites, whose row is not drawn (`CA_0055_005`), and a new block where it would land; a removal and a move frame the block that stands, which stays the editor's — a removal strikes its text through, and a move is marked where it stands with Phosphor `arrows-down-up`, where it would go said in its accessible name (decided 2026-09-10). Retired rows stay dashed and struck, so the three treatments stay apart by more than colour (`BO_0233_004`, `BO_0233_005`).
- The chip sits half over the bottom border, left-aligned and inset by the gutter's width, out of the flow: the proposal draws no line of its own for it, and keeps the room it takes below its border whether or not it is shown ([A Marked Block Is A Card](./block-editor.md#a-marked-block-is-a-card), `DO_0008_009`). Nothing stands on the left border or the top edge, so a framed row's ⠿ handle and standing glyph at the top of its left edge are never covered (`BO_0265_010`, replacing `CA_0043_001`'s face placement). On a phone the chip starts 0.35rem inside the border and may take the width inside it, and the words give way first, ellipsed, before the face and the answers.
- Editing accepts through one path. A proposal's text cancels every input as it begins — synchronously, since a `$`-handler's `preventDefault()` runs after the browser has changed the text — and `heldEdit` says what it would have done; the item is accepted through `answerProposal`, the document re-read, the established block activated at the same offsets, and the held edit applied through the editor's own `editRuns$`, which saves it. An input that changes nothing (an undo, a delete at an edge) accepts nothing; a refused acceptance applies nothing and says why (`BO_0233_006`).
- An edit's acceptance says so. `answerProposal` carries `edited` from the command route through `answerDocumentProposal`, and an edited `replace` that CCGW answers as drifted is accepted over the drift (`override`) even when its words, role or place changed; the standing the block holds now is written back when it differs from the proposal's (`restoreStanding`), as for a standing-only drift (`blockDrift`). Without `edited`, and for a move, the `BO_0233_011` refusal stands. `tests/behavior/documents.test.ts` proves it over CCGW: a rewrite is staged, then the block's words are revised and its pin taken back; the plain accept is refused with `proposalStale`, the edited accept establishes the rewrite with the reader's standing, and an edited accept of a stale move is refused (`CA_0042_002`).
- `settleProposal$` is the one caller that accepts as an edit; the icons and a run chip's *Accept all* and *Reject all* answer through `answerProposal$` without it. `proposal-block.test.ts` proves that the settle path's answer carries `edited` and the icons' answers do not (`CA_0042_003`).
- Walked on the served build at pin 338 on 2026-09-11. The user typed into Claude Code's rewrite of a pinned block that they had revised after it was staged: the rewrite was accepted with the typing, and the block kept its pin (`CA_0042_004`).
- Moving uses the editor's own drag and drop marks, with `proposal:<itemId>` as the dragged item; a drop compiles into `placeProposal`, which stages `SET order` into the item's group — a staging into a group that already holds a candidate of the block builds on that candidate, so a rewrite keeps its words (proven over CCGW). Arrow presses on the face draw the new place at once and stage it once they pause for 600 ms, since the kernel refuses a block restaged within 250 ms (`BO_0233_007`).
- `Accept` and `Reject` sit on the item, and a group's `Accept all` and `Reject all` on its run chip (`BO_0265_014`). Accepting answers one item and then reads the document again, so what the reader sees is what the graph holds rather than what the surface predicted; `Accept all` and `Reject all` run the same per-item answer over the group in its own order, which is what makes them a shortcut rather than an operation with acceptance semantics of its own (`CA_0023_013`, `answerGroup$`).
- `tests/browser/block-editor.spec.ts` proves the treatment on desktop and mobile against the real application: a staged group counted in the panel while the document still reads as truth, the toggle showing the change against the block it concerns and below it rather than over it, one item accepted leaving the block holding it and the rest of the group standing, one rejected leaving its block unchanged, a whole group accepted closing it and taking the treatment away, a retired block and a proposed change on screen together each saying in words which it is, and the axe scan clean with a proposal showing. The proposals are staged through the transport's own `propose` command, because no surface stages one: staging is what an untrusted writer does and this view only answers it (`CA_0023_012`, `CA_0023_013`).
- One proposal group carries both vocabularies, and that is what stops the decision server half leaving this extension (found 2026-09-16 under `BO_0256_008`). A `propose` command's `items` array mixes this extension's kinds — `insert`, `replace`, `remove`, `move` — with `calliopa-refine`'s — `relate`, `reason`, `kind`, `claim`, `derive`, `state`, `phase` — one parser reads them and one compiler stages them into a single CCGW script, because a proposal group is atomic by `calliopa-base.working`'s `atomicProposal`: one change is one group, accepted or rejected as a unit, and a run that revises a sentence and asserts the claim it makes must stage both together. So the parse cannot simply be handed to whoever owns each kind: the staging is one script, and splitting it would either break atomicity or mint two groups for one act.
- [ ] BO_0256_012 Decide what follows from that, because it decides how much of `BO_0256` is possible. Three ways: make the item parser and the proposal compiler extensible, so an extension registers the kinds it owns and contributes their statements into the one script — honest, and a mechanism nothing here has; or let a proposal group hold items of several extensions by staging per extension and accepting as one, which is a change to what a group is; or leave the decision *write* half here, moving only the vocabulary, the reading routes and the surfaces, and accept that the Apache-2.0 body carries the statements that write claims and relations while what they mean stays private. The last is the smallest and is the one that admits the separation is partial.

## Taking A Started Document

- Under `BO_0251`, transferred 2026-09-16: a document a run started is taken the way any proposal is, by editing or answering it, and its node goes with the first item the reader accepts.
* A person takes a started document by editing it or by its run chip's *Accept all*, by the edit-accepts rule of `BO_0233` — not by `calliopa-refine`'s answer-by-use. Rejecting every item discards the document. User decision, 2026-09-16.
- The document node goes with the first acceptance (`BO_0251_009`, landed 2026-09-16). `answerDocumentProposal` asks `startedIn(documentId)` — no established revision and a started read — and, for an acceptance in that document's group, decides the document node's member (`node:<documentId>`) before the item's members; so do an edit's acceptance through `settleProposal$` and each step of *Accept all*, which answer through it, and the first of them takes the document while the rest find it established and answer as any insert. Retitling a started document takes it too: `renameDocument`, finding no document in truth, compares the base with the started revision, accepts the node's member, and writes the title on top of the revision the acceptance established. Rejecting an item of a started document reads the document's proposals again, and when that group has none left rejects the node's member, so nothing of the document stays in truth or the listing. **The order is the shell's to keep**: proven in the kernel over the real in-memory CCGW (`internal/kernel/agenttools/start_test.go`, `TestCoreGivenAStartedDocumentThenItsMembersAreTakenDocumentFirst`), the document's member and then each block's leave every block established and contained, and every member rejected leaves nothing — but accepting a block's member before the document's succeeds and leaves the block established while its document is not, so the core does not guard it. `tests/behavior/documents.test.ts` proves it over the kernel harness: the second block accepted first takes the document with it and leaves the first an ordinary insert; a retitle takes the document with the new title while its block stays a proposal; two items rejected one by one leave the document standing after the first and unknown and unlisted after the second. Each rule was shown to bite by removing it.

## Proposals Land As Structure

- Under `BO_0270`, promoted to draft by the user on 2026-09-20 and transferred here the same day: an agent's proposal lands as the structure of what it wrote, not as one block holding the whole answer. The rule is the guide's — `ui.shell.documents`, this extension's `ext.skill` member, which every run working in a document reads (`ui-kernel.md`, `skillsForRun`). The kernel's half is to stop prescribing the shape in the run's instructions (`calliopa-bootstrap`'s `docs/system/ui-kernel.md`, *Proposals Land As Structure*, `BO_0270_001`–`BO_0270_003`, `BO_0270_006`).
* An agent's proposal lands as the structure of its content, not as one block. User request, 2026-09-20.
* The rule is carried by a guide inside Calliopa — an `ext.skill` member — rather than by kernel-composed prose. User request, 2026-09-20.
* Content is split at its headings where it has any, and a heading becomes its own `text` block with the matching role — never `#` characters in a block's runs, which nothing renders. User request, 2026-09-20.
* One block per paragraph: under a heading, each paragraph of the section is its own `text` block, as the editor reads a block. User decision, 2026-09-20.
* A list becomes one block per item, its marker left as plain text; a fenced code block and a table each stay one block, because each is one unit. User decision, 2026-09-20.
* A rewrite carries the same structure: a run rewriting one block into content with headings stages the replace beside the inserts, in one group, with the rationale saying the items belong together. User decision, 2026-09-20.
* The rule reads the same at every length. There is no size below which a run keeps the answer in one block: short content carries no heading and is one paragraph, so it lands as one block by the rule rather than by an exception. User decision, 2026-09-20.
* Structure is never invented: content that carries no heading is not given one. User request, 2026-09-20.
- The reader can reject a rewrite's `replace` and accept its inserts, leaving the rewritten block beside the new sections. This change accepts that: the group's rationale is what says the items are one change, and nothing here makes a group answerable as a unit.
- A list role for the block vocabulary is out of this change. The role set stays `paragraph`, `h1`, `h2`, `h3`, `quote` ([Block Document Model](./block-document-model.md)), so a list item's marker is plain text in its block; widening the vocabulary and teaching the editor to render a list is its own change.
- The guide carries the rule (`BO_0270_004`, landed 2026-09-20). `ui.shell.documents` gained a `structure` convention: what a run stages arrives as the structure it wrote, split at its headings — a heading block per heading with its role, a block per paragraph, a block per list item with its marker as plain text since no role renders a list, a fenced code block and a table each whole, no invented structure, and the same rule at every length — and a rewrite staging the replace beside its inserts in the one group with the rationale saying they are one change. Its `prompts` convention stages a question's answer directly after the command block as the blocks the reader should read it as, where it said *one insert*. Three examples show it: `answerAQuestion` is the short answer in one block, `answerWithSections` is one item per block each placed after the command block, and `rewriteIntoSections` is a replace beside its inserts. `outputContract` is unchanged — one call per coherent change. Revised through `kernel commit --members` in this change's proposal.
- Walked on the instance on 2026-09-20 at pin 664–670 (`BO_0270_005`), four runs on Claude Code through a probe account since retired, each aimed at the `Test` document. A command whose answer has two sections and a list (`arun-2c8f0c6459bb6774`) staged eight blocks: two `h2` heading blocks, a paragraph under each, and one block per list item with its `-` marker as plain text, with no `#` characters in any block's runs. A one-sentence question (`arun-8d4951df266c1d42`) staged one block, by the rule rather than by an exception. A rewrite into sections (`arun-6b230e82882752dd`) staged the replace of the named block beside a new `h2` and a new paragraph in the one group. A run sent from a block (`arun-c152578638c2ac8b`, from the prompt *what does #1 mean?*) staged its answer at order key `t`, directly after that block at `r`.
- A table arrives as a table block (`BO_0287_016`, landed 2026-09-23, `calliopa-bootstrap`'s `BO_0287`): the `structure` convention of `ui.shell.documents` says a table is a `table` block with its typed columns and rows, one block whole, never pipes in a paragraph; that a change to a table is a replace carrying the whole table; and that a replace of a table with a file behind it drops the file, since the cells the run proposes are the whole table it read — `addATable` and `addAColumnToATable` show it, revised through `kernel commit --members` in this change's proposal. The proposal row draws a proposed table read-only (`TableBlock`), and a rewrite of a table that a file stands behind says on its row that accepting drops the file (`data-proposal-drops-file`), so the person reads it before the press (user decision, 2026-09-23).

## A Run Works With What Is Already Proposed

- Under `calliopa-bootstrap`'s `BO_0271`, promoted to draft by the user on 2026-09-20 and transferred here the same day: a run working in a document that holds unanswered proposals reads them and refines what stands, rather than writing new content beside it. The kernel's half — what the run is told, what the document tools answer, and how a refinement stages — is `calliopa-bootstrap`'s `docs/system/ui-kernel.md`, *A Run Works With What Is Already Proposed* (`BO_0271_001`–`BO_0271_008`).
* A run reviews the proposals standing in the document and refines the ones that need it, rather than only adding new content. Any proposal the person has not answered is open to discussion. User request, 2026-09-20.
* A run refines by building on the candidate that stands, never by staging a second one beside it: a block keeps at most one open proposal, and the person answers once. User decision, 2026-09-20.
* The run stages only what needs changing. Where a standing proposal already says what the run would say, it leaves those words as they are; it says so only when there is really nothing to add anywhere. User decision, 2026-09-20.
* A refined proposal is the refiner's work with its origin kept: the chip carries the refining run's face and words and says it refines an earlier proposal, naming its proposer. User decision, 2026-09-20.
* A proposal is refined once its run has ended; a run still staging is left to finish. A member the person has already answered is settled and is not refined. User decision, 2026-09-20.
* A person's branch is refined only by the run working in it; another person's branch, a system run's derived candidates and a fixated framing are read as context and never proposed against. User decision, 2026-09-20.
* A commit command is answered as it is today — one phase item on the document — and its one sentence of consequences names the proposals standing unanswered in it. User decision, 2026-09-20.
- A run never answers a proposal: accepting and rejecting stay the person's, as they are for everything a run stages.
- The guide carries the rule (`BO_0271_009`, landed 2026-09-23). `ui.shell.documents`' `openProposals` convention: the document's unanswered proposals are read before anything new is written — `read_document` answers each block's `proposal` and the document's `proposedInserts` — and what stands and needs sharpening is refined in place with a `replace` carrying `refines: {group, item}`, never proposed again beside it, since a block keeps one open proposal; only what needs changing is staged, words that already say the thing are left as they are and said to be left only when there is nothing to add anywhere; the document's own blocks are left alone unless the command referenced one or plainly asks for those words to change; a proposal whose `refinable` is false — a run still going, another person's branch, the system's derived candidates, a fixated framing, an answered member, a removal or a move — is context and is never proposed against. `permittedOperations` names what the reads answer about a standing proposal and the `refines` field; the `commit` convention and `outputContract` say the sentence of consequences names the proposals standing unanswered, and that a refinement is reported as what it refined and whose it was. Two examples show it, `refineAStandingProposal` and `leaveWhatAlreadySaysIt`. `calliopa-base.working` carries the staging half as `oneOpenProposalPerBlock`. Both revised through `kernel commit --members` in this change's proposal. What the run is told and what the tools answer is the kernel's (`calliopa-bootstrap`'s `docs/system/ui-kernel.md`, `BO_0271_001`–`BO_0271_004`).
- A refined group is read as the refiner's (`BO_0271_010`, landed 2026-09-23). The kernel writes `refinedBy: run:<id>` on the candidate a refinement builds on, so the record is the group's own candidate, not the refining run's node — it stays with the item whatever becomes of the refiner's group, and a group a second run built on reads as one item with the refined words. `readDocumentProposals` reads the stamp on a rewrite or an insert as `refinedBy: {runId, proposer}` (`refinedByOf`), resolving every refiner's proposer from the `agent.run` nodes in one read after the groups are read; a refiner still going has no node yet and answers an agent of `null`, which the editor resolves from the run's activity (`refinerOf`, `lib/agent-at-work.ts`). The group's own `proposer` stays whose proposal it was.
- Walked on the instance on 2026-09-23 (`BO_0271_012`), on the document *working with ai* through a probe account on Claude Code, with the kernel rebuilt and the editor's read run against the live graph. A referenced command rewrote its block in the run's own group; an unreferenced command bearing on that block refined the standing rewrite in place — the block kept one candidate, in the first run's group, stamped `refinedBy` with the second run — so the person answers once; a command the standing rewrite already satisfied staged nothing and said so; two summary asks, with and without the block referenced, inserted a sentence and left the accepted words alone, while an ask to add a closing sentence to a section was answered by appending to its last paragraph, which the run read as a plain ask to change that section; and a run that read the document while another was still staging refined that run's rewrite only after it had ended. `readDocumentProposals` read each refined item with its `refinedBy`; once the kernel counted a refining run as having staged, so that its provenance landed, the refiner resolved to Claude Code, and the earlier refiners of the walk resolve to no agent, since their runs left none. The chip in the served shell follows the pin's promotion.

## A Run Proposes To Withdraw A Proposal

- Under `calliopa-bootstrap`'s `BO_0286`, promoted to draft by the user on 2026-09-23 and transferred here the same day: a run may propose that a standing proposal be withdrawn — marked as superseded by its own refinement or by nothing — and the person answers it with one press. The kernel's half — the `withdraw` item, what the reads answer about it, and what the run is told — is `calliopa-bootstrap`'s `docs/system/ui-kernel.md`, *A Run Proposes To Withdraw A Proposal* (`BO_0286_001`–`BO_0286_006`).
* A run may withdraw any standing proposal it may refine, of any refinable kind; a withdrawal is a proposal and never an answer: the item stays open with its words, and the person rejects it with one press or accepts it as before. It may name a successor and carry a reason of one short line. User decision, 2026-09-23.
* Accepting a successor rejects, in the same press, every withdrawn item that names it; a withdrawn item without a successor, or whose successor was rejected, stands with its mark until answered on its own chip. User decision, 2026-09-23.
* A withdrawn item stays counted among the document's unanswered proposals. A withdrawal is a mark on another group's item, not an item of the withdrawing run's own group, so that run's chip counts the successors it staged and its *Reject all* leaves every withdrawn item standing with its mark; its *Accept all* accepts the successors and so rejects what they withdraw. User decision, 2026-09-23.
- The guide carries the reconciliation rule (`BO_0286_007`, landed 2026-09-23). `ui.shell.documents`' `openProposals` convention says that when the command asks to reconcile, merge or reduce the standing proposals, the run refines the ones that stay, withdraws the ones the reconciled words make surplus with a `withdraw` item — `proposal: {group, item}`, `successor` as a standing item or the index of an item in the same call, `withdrawalReason` where the successor does not say it — and stages nothing beside them; that a withdrawal is a proposal the person answers, accepting the successor rejecting what it supersedes; and that the final words say how many were refined and how many withdrawn, in whose favour. `permittedOperations` names the item and what the reads answer as `withdrawable` and `withdrawnBy`, `outputContract` the report, and the example `reconcileStandingProposals` shows twenty proposed blocks reconciled into six. Revised through `kernel commit --members` in this change's proposal. The item's fields are `successor` and `withdrawalReason` because the item schema already carries `supersededBy` for a root's phase and `reason` for a relation's.
- A withdrawn item is read with its mark (`BO_0286_008`, landed 2026-09-23). `readDocumentProposals` reads the kernel's marks — `withdrawnBy`, `withdrawnFor`, `withdrawalReason` — on a rewrite, a move, a proposed insert and, for a removal, on the group's carry-forward anchor of the retired block, as `withdrawal: {runId, proposer, successor, reason}` (`withdrawalOf`); the proposer is resolved from the withdrawing run's `agent.run` node in the one read `BO_0271_010` added for refiners, an agent of `null` until it lands, which the editor takes from the run's activity (`withdrawerOf`). Every withdrawn item stays in its group's items and in `unanswered`; a run's chip counts its own items, which its withdrawals are not.
- The successor's press withdraws (`BO_0286_009`, landed 2026-09-23). `answerDocumentProposal` (`server/documents.ts`), after accepting an item, reads the document's standing proposals and rejects every item whose `withdrawal.successor` is the accepted item, one `REJECT PROPOSAL p MEMBER m` per member in that item's group; an item that could not be rejected stays with its mark and is named in the answer's `notice`, which `answerProposal$` puts in the editor's notice, and the answer carries `withdrawn`, the count. Every path that accepts — the chip, the bar, a swipe, an edit, *Accept all* — goes through it. Before the press, the successor's chip says what else it does: `withdrawnCountOf` (`lib/agent-at-work.ts`) counts the items naming it, and `proposalNames` reads *Accept Codex's move, withdrawing 1*. A withdrawn item is one press away on its own chip as before.
- Walked on the instance on 2026-09-23 (`BO_0286_010`), on *Make it work* through a probe account on Claude Code and then by the user at pin 1946. The command that found this change — *reconcile the standing proposals into one coherent argument*, over thirty proposed blocks from three ended runs — was answered by two refinements and fourteen withdrawals, each with a reason and nine naming a successor, with fifteen blocks left as they were because their words already said it and nothing staged beside them; the run's own group held only its summary. The editor's read resolved every withdrawer to Claude Code and counted the successors. The user then saw the withdrawn blocks standing dashed and dimmed, pressed a withdrawn block's link to bring its successor into view, accepted the successor — the two withdrawn blocks naming it went with it in the same press — rejected a withdrawn block from its own chip, and accepted two withdrawn blocks anyway, which stand established with their marks. Two things the walk found were fixed on the way: nothing showed withdrawn at rest, because the text element sets its own colour and the chip is hidden at rest; and the successor stood in a change the chip line was not showing, with the count only in the accept's accessible name — which became the link control and the visible count (`BO_0286_012`).

## A Proposal Takes A Standing

- Under `calliopa-bootstrap`'s `BO_0272` (2026-09-21), a proposed change can be swiped as a block of the document can: the swipe accepts it, and the block it becomes takes the standing the release committed. The scale itself is [Block Editor View](./block-editor.md#standing).
* A proposed rewrite, insert or move can be swiped, and the swipe accepts it before the block it becomes takes the standing. A removal, a work item and the *Possible relation* card keep their answer icons and take no swipe: accepting a removal retires its block, so nothing is left to carry a standing, and the others are not text blocks. User decision, 2026-09-21.
* A discard swipe on an agent's proposal accepts it and discards the block, and a system run's derived candidate is no such exception since answer-by-use went: it is a row the swipe may move like any other, accepted and then given the standing the release committed. User decisions, 2026-09-21 (`BO_0272`, then `BO_0258_015`).
* The gesture wins over the standing the block already carries, on a rewrite or a move, and on a rewrite of a fixated block: the swipe is the reader letting that rewrite in, and it is the most recent thing they said. This narrows *a pinned block is never overwritten* (`BO_0246`) to what a run stages, never to what the reader's own gesture accepts. User decision, 2026-09-21.
* A proposal's standing is set in reading mode by the swipe and, on the proposal the reader has turned to, by the standing toolbar it carries there, which is the path that is not the gesture. User decisions, 2026-09-21 and 2026-09-23 (`DO_0014`).
- An acceptance that is refused — stale, conflicted or drifted — writes no standing, and the refusal is said where the proposal is read. *Take back* takes back the standing alone: answering a proposal is a saved operation the undo keystroke does not reverse, and this gesture does not make it one.
- The swipe reaches a proposal (`BO_0272_008`). `rowOf` in `views/block-swipe.ts` takes a row carrying `data-proposal-id` whose kind is one of `SWIPEABLE_PROPOSALS` — `replace`, `insert`, `move` — beside a text block's reading row, reading its standing as keep, the state the block it becomes would be in; the committed swipe names what it was made on (`Swiped`). `swipeProposal$` in `block-editor.tsx` answers `answerDocumentProposal` first and, on success, writes the standing on the block the acceptance established — the item's own, which a rewrite and a move keep and an insert creates — through `use-standing.ts`, so the announcement, *Take back* and the read-back follow. A refusal writes nothing and says why; a derived candidate keeps `settleDerived$`, and an item already answered or edited into is left alone. `ProposalBlock` draws `StandingButtons` on a swipeable proposal in command mode, wired to the same write.
- Verified 2026-09-21 (`BO_0272_012`), `views/proposals/proposal-standing.test.ts` in the render harness: the three kinds carrying the buttons and a removal carrying none, *Fixate* on a proposed insert accepting it and fixating the block it became, *Discard* on a rewrite accepting it and discarding its block, a refused acceptance writing no standing and saying so, and `rowOf` taking a rewrite, an insert and a move and leaving a removal alone. Each was shown to bite: with the standing write removed two cases fail, with the kind guard removed the removal carries buttons, and with `rowOf`'s guard removed it takes the removal too.

## A Derived Candidate Is Answered

Under `BO_0258` (`calliopa-refine`'s `docs/changes/BO_0258_FEAT_quiet-refinement.md`), promoted to
draft by the user on 2026-09-21 and transferred the same day: refinement runs only when a person
asks for it, and what it proposes is answered explicitly. The rest of the change is
`calliopa-refine`'s `docs/system/system.md`, Refinement Is Invited, and the fixed layer's
`ui-kernel.md`.

* A derived candidate is answered explicitly, like every other proposal: it carries answer controls, and acceptance by fixating, editing or referencing it ends. What made answer-by-use right was that derived candidates arrived unrequested; under `BO_0258` the person asked for them, and there are few. User decision, 2026-09-21.
* A derived candidate then takes the swipe like any other proposal, so the carve-out of *A Proposal Takes A Standing* — which rests on answer-by-use — is removed rather than kept. This lands after `BO_0272`; until it does the carve-out stands and nothing there is false. User decision, 2026-09-21.

- Answer-by-use has ended (`BO_0258_014`). A derived candidate draws the answer icons every candidate draws, with *Fixate this framing* and *Discard* beside them — fixate accepts and stands it in one press, discard rejects it (user decision, 2026-09-21). What no longer accepts it is using it: referencing it in a command, or giving it a standing by a path that did not answer it, is refused and the reader is asked to answer it first (`unanswered$`, and the `beforeReference$` and `beforeStanding$` hooks, which refuse where they used to accept). The hooks stay rather than go, because they are also what keeps a standing or a reference off a candidate truth does not hold yet.
- The derived carve-out has gone from the one path that accepts and then acts (`BO_0258_015`, `acceptThenAct$`): the swipe and a press in the bar's block groups both reach it, so both now work on a derived candidate. The acceptance marks the item answered before the act runs, so the act is never the thing that answered it.
- A derived block a person took as their own is challenged beside, never rewritten (`BO_0258_016`). `assemble.ts` carries `revisedBy` — who made the revision the block holds — beside `revisedAt`, so the editor tells a block a run maintains from one a person edited without a second read; `governedBlock` in `block-editor.tsx` is the test, symmetric with `fixatedBlock`, and `derivedNames` names what a rewrite challenges: the fixated framing, or *the reader's own words* where a person made them theirs. The fact is `calliopa-refine`'s (`BO_0258_008`); this is `documents` reading it off the block and saying so. A rewrite already stands beside the block it concerns rather than replacing it, so the reading order needed nothing.

## The Bar Acts On A Proposal

- Under `documents`' `DO_0006` (2026-09-21), the bar's block groups come up on a focused proposal as they do on a block ([Block Editor View](./block-editor.md#the-bar-before-editing)).
* A proposed block is worked with as an ordinary block: focusing it brings *Standing*, *Block* and *Turn into* up, and a press in one of them accepts the proposal first and then acts on the block the acceptance established. This is the grain typing into a proposed block already has, and rejecting stays the way a proposal goes away. User decision, 2026-09-21.
* The *Block* group's *Add …* controls are the exception: they leave the proposal open and place the new block directly below where the proposal is drawn, so it reads below it before and after the proposal is answered ([Block Editor View](./block-editor.md#a-new-block-lands-below), `DO_0016_003`). User decision, 2026-09-24.
- The bar is a third path to a proposal's standing, beside the swipe and the standing toolbar on the proposal turned to (`BO_0272_008`, `BO_0272_010`, `DO_0014_003`).
- A press in a block group on a focused proposal (`DO_0006_004`, landed 2026-09-21). `ProposalBlock` tells the editor the reader has turned to it (`focus$` from its `onFocusIn`), which makes the proposal the bar's subject and lets the focused block go; the two focuses are exclusive. `acceptThenAct$` is the one accept-then-act path for every act but *Add …*, which places below the proposal and answers nothing (`DO_0016_003`), and `BO_0272_008`'s swipe now calls it: the guards are its own — a kind outside `SWIPEABLE_PROPOSALS` has no block to act on, and an item already answered or edited into is left alone; the derived exception went with answer-by-use (`BO_0258_015`) — then the acceptance, the drop, a read of the document, and the named act on the block it established (`item.blockId`: the item's own, which a rewrite and a move keep and an insert creates), which is then the focused block. A refused acceptance acts on nothing and says why where the proposal is read. A proposal shows the resting standing until it is accepted, since it carries none.

## The Relation Card Answers In Its Chip

Under `documents`' `DO_0007` (`docs/changes/DO_0007_FEAT_relation-card-answers-in-the-chip.md`),
set to draft by the user on 2026-09-21 and transferred the same day: a proposed relation is answered
where every other proposal is answered.

* The *Possible relation* card's two answers are the chip's, drawn as an agent's proposal draws them — the ✓ and the ✗ in the chip on the card's bottom border, at the chip's size — and the card's own row of buttons goes. They come up with the chip, hovered or tapped once, as every proposal's answers do. User decision, 2026-09-21.
* *Edit reason* goes. The reason is editable where it is read, so a press in it puts the caret there, and an edit left or paused still accepts the item with the edit on top. User decision, 2026-09-21.

- The chip carries the card's answers (`DO_0007_001`, landed 2026-09-21). `ProposalBlock` draws `proposal-block__answers` for an inferred relation as it draws them for an agent's proposal — `data-proposal-accept` and `data-proposal-reject`, the ✓ and the ✗ at the chip's size — so the card is answered where every proposal is answered, and it comes up with the chip: hovered, focused, or after a phone's first tap (`DO_0004_004`, `DO_0006_010`). The icons carry the card's own words, `INFERRED_RELATION_NAMES` in `lib/proposals.ts` merged over `proposalNames`, so the decision is the library's and the drawing only draws it.
- `PossibleRelation` has no controls of its own (`DO_0007_002`): its row of buttons is gone with *Edit reason*, the host ref that button needed to reach the reason, and the `.proposal-block__card .proposal-block__use` rules in `block-editor.css`, which leave `.proposal-relations-more button` — the line that opens the relations beyond the first two — the card idiom's only button. The reason is unchanged: `contenteditable` where it is read, a press putting the caret in it, and a pause or a leave accepting with the edit on top.
- Proven in the render harness (`DO_0007_003`): `views/proposals/proposal-block.test.ts` and `calliopa-refine`'s `views/decision/relation-card.test.ts` press the chip's icons — the card drawn with no buttons of its own and its reason still editable, the icons named *Confirm relation* and *Not related*, the ✓ accepting without `edited` and the ✗ rejecting. Each was shown to bite: with the names merge taken out the icon reads *Accept the system's relation*, with the chip's carve-out put back the icons are absent and the press times out, and with a button row put back on the card the *no buttons of its own* assertions fail. Verified on the tree at dataRevision 842: `tsc --noEmit` clean, 119 unit files and 1035 tests, both bundles built.
- `calliopa-refine`'s `docs/system/system.md` follows in the same proposal group, since it names what the card offers (`DO_0007_004`).

- Walked on the served build at pin 846, 2026-09-21, and the user accepted it (`DO_0007_006`): an inferred relation's card under its source block, answered from the chip on its edge — the ✓ confirming and the ✗ dismissing — and the reason taking the caret from a press in it. The card offers nothing else, and a phone reaches the chip on the first tap as every proposal's.

## A Proposal Carries Its Standing While Reading

- Under `DO_0014`, set to draft and then ready by the user on 2026-09-23 and implemented the same day: a proposal's three standing buttons move out of command mode with every other row's, and are drawn while reading on the proposal the reader has turned to. The rule and the rest of the change are [Block Editor View](./block-editor.md#standing-belongs-to-reading) (`DO_0014_001`–`DO_0014_009`).
* A proposal is a subject like any other row, so it carries the buttons when it is focused or holds the caret, and carries none in command mode. Both of a proposal's paths to a standing — the swipe and the buttons — are reading's, which replaces `BO_0272`'s *in reading mode by the swipe and in command mode by the standing toolbar it now carries*. User decision, 2026-09-23.
- A proposal's buttons are drawn for the first time (`DO_0014_003`, landed 2026-09-23). `views/proposals/proposal-block.tsx` draws `StandingButtons` while reading, on the proposal carrying `data-focused="true"` or named by `state.editingItemId`, which the editor now hands it as `editing`. `BO_0272_010` had put the control inside `.proposal-block`, where no rule ever lifted it out of `display: none` — the only two named `.block-row`, and a proposal row is a sibling of the block rows, never inside one — so it had never been visible in a browser; its suite asserted the markup and pressed through the DOM, which passed with the control unseen. The toolbar is no longer hidden at all, so the defect has no way back (`DO_0014_002`).
