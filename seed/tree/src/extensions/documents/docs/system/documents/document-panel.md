# Document Panel

## The Document Metadata Panel

* A document's own facts live in the inspector, contributed through the view contract: its save state, when it last changed, how many times it has changed, and how many proposed changes stand unanswered against it. The operations that act on it as a whole — deleting it, and revealing its retired blocks, its discarded blocks and its proposed changes — are on the document's bar ([Block Editor View](./block-editor.md#presentation)), and the inspector contributes no action. User decision, 2026-09-18 (`CA_0053`).
- No panel says a document is a change of an extension and none carries a status: a change document is not a document but an `ext.source` member of its extension, read-only in the extension view, and its status is the `Status:` line of that file, established by the user accepting the proposal that changes it. The rule that only a person establishes `draft` or `ready` is honoured by who accepts, and an agent still never establishes a status. User decision, 2026-09-16 (`BO_0254_011`).

- The save state is the one this view already reports on the bridge, in words: `saving`, `saved`, or `unsaved`. There is no connectivity signal and no local draft, because offline editing is out of scope, and `unsaved` covers a failed save as well as an untried one.
- The header renders the same channel. The panel adds no second notion of whether work is safe; it states the one channel beside the document's other facts, where a reader asking "is this saved" is already looking. Which block a failure happened to stays on that block, because a document-level state cannot name one.
- The last-changed time is an absolute timestamp in a `time` element carrying the machine-readable value. A relative age in a drawer that sits open either keeps itself current or states something false, and a static absolute value cannot go stale.
- The count and the time are the ones [Block Document Model](./block-document-model.md) defines. They cover the whole document — its title, its blocks' content, their order, and retirement and restore — rather than the `document` node alone.
- This is a count and a timestamp, not a revision browser. A list of revisions the reader can open or read back stays deferred.
- The panel follows the active tab, like everything else in the inspector. A tab holding no document shows what that tab's view contributes.

## Deleting A Document

* The document's bar carries a `Delete` control that removes the document from the product. It asks first, and the confirmation names the document by title.

- Deleting is irreversible from where the reader stands. [Block Document Model](./block-document-model.md) archives rather than removes, so the history survives, but nothing in the interface brings a deleted document back. It is the one destructive operation no undo covers, which is why it asks.
- Deleting closes every tab in this workspace showing that document, so the reader is not left editing something that no longer exists.
- A document holding unsaved input deletes anyway, with no warning beyond the confirmation. Deleting what was never saved reaches the same outcome as deleting what was, so a second warning would mark a distinction with no consequence.
- The view drops that pending input rather than flushing it. The unmount flush exists so a tab switch loses nothing typed; flushing into a document being archived in the same breath would write a revision whose only purpose is to be buried.
- Cancelling changes nothing.
- The confirmation is a message on the shell's message surface, raised through the view bridge rather than rendered here. This view supplies the words and what each answer does, which is what the fixed line in [Shell Integration](./block-editor.md#shell-integration) already requires of it: it never takes ownership of a shell surface. [Workspace Shell](../workspace/messages.md) owns the surface, and a raised message holds the floor until it is answered.

## Retired Blocks In Place

* The retired-blocks toggle is on the document's bar. Turning it on shows the retired blocks in the document, at the positions they held when they were retired.
* A retired block shown in place is read-only. It cannot be activated and cannot be edited; the only thing it offers is `Restore`.

- There is no separate retired list. The document itself is the list, which is what makes the position mean anything.
- Position comes from the order key the block held when it was retired. The key lives on the block and its last revision still carries it, so a retired block interleaves with the reading order by the same string comparison siblings already sort by. A key that no longer falls between two current siblings still sorts somewhere by that comparison, and that is where it appears.
- The position is where the block was, not a promise about where restoring would put it. [Block Document Model](./block-document-model.md) mints a fresh key on restore, so a restored block lands where restore chooses, and it moves for that reason rather than because the toggle was lying.
- A merge retires the block it absorbed, so turning the toggle on after a merge shows the absorbed block beside the text that now contains it. That is a truthful account of what happened and reads as duplication, so the retired presentation has to be unmistakable.
- A retired block is visibly distinct from a block in the reading order by more than colour, and carries an accessible name saying it is retired. A reader scanning the document with the toggle on must never mistake one for content.
- The toggle is off by default and its state is the tab's, so turning it on in one document does not turn it on in another.
- This does not contradict the content-only reading surface. That rule governs the reading order, which retired blocks are not part of: they are visible only while the reader has asked for them, which is an inspection the reader turned on rather than chrome that arrived uninvited.
- The document read and the retired read stay separate requests. Turning the toggle on reads the retired blocks then, so what is interleaved is current rather than left over from an earlier state of the document.

## Implementation

- The view contributes the document's panel through the widened contribution: its save state in words, its last-changed time, and its revision count. `report$` is the one place a save state is reported, so the header and the panel state the same value rather than each deciding for itself (`CA_0015_008`).
- The count and the time refresh in a task of their own, tracking the document read and the reported save state. Neither the read nor the save fetches them: both are on the path leaving a block takes, and a panel-only request there delays the write that records which block the tab was left on (`CA_0015_008`).
- `Delete` on the bar raises a message on the shell's surface, naming the document by title. The title is read as the control is pressed rather than as the contribution is built, so the question names what the document is called now (`CA_0018_001`). Confirming deletes through [Block Document Model](./block-document-model.md) and then tells the shell the target is gone, which closes every tab in this workspace showing it. Cancelling changes nothing (`CA_0015_009`).
- Pending input is dropped rather than flushed: the editor is reset before the delete is sent. The unmount flush exists so a tab switch loses nothing typed; flushing into a document being archived in the same breath would write a revision whose only purpose is to be buried (`CA_0015_009`).
- `readingOrder` merges the reading order with the retired blocks the toggle reveals, sorting both by the order key each block carries and putting a block with no usable key after them by identity. It is pure, so where a retired block lands is settled without a document (`CA_0015_010`).
- A retired block renders as its own row, not a block row: a dashed card, struck text, `retired` on its top border ([A Marked Block Is A Card](./block-editor.md#a-marked-block-is-a-card)), and a group name saying it is retired. It carries no reading affordance and no editable element, so it cannot be activated or edited, and the only control it offers is `Restore` (`CA_0015_010`).
- A document whose reading order says nothing still shows what the toggle revealed. The blank-page placeholder stands for a document with nothing to read, and a document showing retired blocks the reader asked for is not that (`CA_0015_010`).
- The toggle is on the bar, is off by default, and its state is the view's, so it does not follow the reader into another document. Turning it on reads the retired blocks then, so what is interleaved is current rather than left over (`CA_0015_010`).
- The status is a `choice` action, the third kind of the view's action vocabulary: id, label, the current value, the options with their labels and the icon that stands for each, and `run$(value)`; the shell renders it as a labelled native select in the inspector and the dock alike. The editor contributes it, with a *Change of* text fact, for a document carrying `change`, and choosing sends the `setStatus` command with the document's revision; a stale base is answered as a conflict and the document read again rather than overwritten, a landed write updates the held revision and asks the shell to re-read the sections listing the document (`bridge.targetChanged$`) (`BO_0222_007`).

## Discarded Blocks In Place

* The bar's *Show discarded blocks* draws each block the reader discarded where it sits, dimmed and named as discarded, offering *Reopen*, which returns it to keep. With the toggle off a discarded block is not drawn at all (`BO_0227`).

- In place rather than in a count at the end of the flow, for the reason [Retired Blocks In Place](#retired-blocks-in-place) gives: the position is what makes it mean anything (decided 2026-09-10). A discarded block differs from a retired one in keeping its place in the document's order, which is why it comes back where it always was, with one press or one swipe; retire takes a block out of the order and brings it back through *Restore*.
- A discarded block is markable where it is drawn, by its words ([Marking Every Drawn Row](./command-mode.md#marking-every-drawn-row), `BO_0263`), and is drawn visibly neither as the flow nor as a retired block's dashed, struck-through card: a dotted card, dimmed, labelled `discarded` on its top border ([A Marked Block Is A Card](./block-editor.md#a-marked-block-is-a-card)). `readingOrder` in `src/components/views/reading-order.ts` decides which blocks are drawn over the reading order and the two toggles (`BO_0227_014`).

## The Root's Phase

The transition card and the marker under the intent, the shell half of `BO_0249` (`block-document-model.md`, The Root's Phase, for the declaration and the write; `ui-kernel.md`, Acceptance As A Phase Transition, for the run's item and the change context).

* Acceptance is deliberate: language may initiate it and the system answers inline with the consequences named; one explicit confirmation completes it, and language alone never mutates shared state (material §20, §46, rule 19). The press on *Establish* is that confirmation (`BO_0249`, Decided).

- The marker under the intent (`BO_0249_008`; `src/lib/phase.ts` `phaseOf`, `phaseWords`): the root's line (`.document-phase`, `data-root-phase`) carries its phase where it is consequential — *Proposed* when `phase` is absent or `proposed`, *Accepted*, *Superseded by «title»* with the successor's title read once from its document and a button (`data-phase-successor`) that opens it in this tab as the pressure layer's source does — and *Proposed* is the one control (`data-phase`, `openPhaseCard$`): pressing it opens the transition card; *Accepted* and *Superseded* are words (`data-phase-words`). A change document carries no phase and no line beyond its status: its `changeStatus` is its lifecycle (user decision, 2026-09-15, `ui-kernel.md` `BO_0249_005`).
- The transition card (`BO_0249_009`; `PhaseCard` in `src/components/views/phase/phase-card.tsx`, `data-phase-card`, the reading `cardReading` in `src/lib/phase.ts`), drawn under the intent when *Proposed* was pressed or a run proposed the transition: *Establish this as the accepted direction?*, then *This would:* and the consequences read (`block-document-model.md` `BO_0249_007`, `fetchConsequences`, read on the press and never with the document) as a list in words (`data-phase-consequence`; `consequenceWords`: the relation's verb, the far block's opening words and its document where it is another's — *constrain traversal security in Security* — a standing judgement as *reopen: «words»*), or *Nothing else in the network rests on this yet* (`data-phase-lead`); a run's sentence on its own line (`data-phase-sentence`) when the card is its proposal; one control, *Establish* (`data-phase-establish`), beside *Not yet* (`data-phase-not-yet`). An accepted root that contradicts this one is named — *«title» is the accepted direction and contradicts this* (`data-phase-conflict`) — and the control reads *Establish and supersede «title»*, sending `supersede`. *Establish* sends `setDocumentPhase` with the document's revision as the base (`sendPhase`, `establishPhase$`), a refusal named on the card (`data-phase-refusal`) with the card left standing, and on success the document is read again so the line reads *Accepted*; *Not yet* closes the card and writes nothing. The two ways in end in the same card: from the marker, and from a run's `phase` item (`proposed-changes.md` `BO_0249_011`), where *Establish* accepts the member through the proposal answer and *Not yet* rejects it.
- The policy hook in the card (`BO_0249_010`; `policyReading` in `src/lib/phase.ts`): the consequences read says whether the signed-in person is `permitted` under the root's policy — the instance owner and any human account the instance admits under the default `owner` rule, or the policy the root's `acceptedBy` relation names — and which `policy` it is; permitted, the card offers *Establish*; not permitted under `owner`, the card stands without a control and says *Only the owner may establish this* (`data-phase-note`); not permitted under another policy, the control reads *Propose acceptance*, which stages the `phase` item as a proposal for the person that policy names to accept in place — unreachable today, since `owner` is the one policy declared, and decided in the pure reading so the card needs no change when another is. `BO_0212`'s separation of duties applies as it does elsewhere.
- Verified 2026-09-15 on the tree (`BO_0249_013`): `src/lib/phase.test.ts` proves the phase read, the marker's words, the consequences in words, the empty card, the conflict and the three policy readings; `src/components/views/phase/phase-card.test.ts` proves in the render harness the marker as the control and nothing read for it with the document, the card's consequences read on the press and listed, the empty case, *Establish* writing from the document's revision and the line reading *Accepted*, *Not yet* writing nothing, the conflict named and the supersede sent, a refusal named on the card, the unpermitted and the other-policy readings, and a run's `phase` item drawn as the card — never a proposal block — with its sentence, accepted and rejected through the proposal answer (the harness answers `GET documents/[id]/consequences` from a fixture and records `setDocumentPhase`). The behaviour tests over CCGW are `block-document-model.md` `BO_0249_007`'s.
- Walked on the served build 2026-09-15 (`BO_0249_014`, the runs in `ui-kernel.md` `BO_0249_004`): on *Walkthrough*, *Proposed* pressed opened the card with its consequences and *Establish* wrote the phase, the line reading *Accepted*; on *Testing Doc* a run's `phase` item arrived as the card with the run's sentence and *Establish* accepted the member. The walk found and fixed: far claims no block asserts listed as consequences (`block-document-model.md`), a person's run on a document without an intention reading no documents skill (`ui-kernel.md`, Commands From The Open Document), the commit convention's reading of a marked block (`tool-access.md`), and the document's single open-candidate rule refusing the phase beside groups that anchor inserts at it (`ccgw.md`).

- Under `calliopa-bootstrap`'s `BO_0274`, transferred here 2026-09-21: the line stops being the
  control and stops being drawn for a document nobody has decided anything about, and the card
  reports what the press took and what it could not. The phase's chrome is drawn by
  `calliopa-refine` through the decoration slot (`block-editor.md`, `BO_0256_007`), so the files
  these tasks name are that extension's `views/phase/` and `views/decision/provider.tsx`; the
  behaviour is the document's and is enumerated here.
- The line says only what is true (`BO_0274_007`). `phaseOf` answers null for an absent or
  `proposed` phase, so a document nobody has decided anything about draws no line at all and
  *Proposed* has left the reader's vocabulary. A decided root's line reads *Accepted*, naming each
  kind that applies with the empty clause dropped — *Accepted · 2 changed*, *Accepted · 3 blocks
  edited since*, *Accepted · 1 changed, 2 blocks edited since*, *Accepted · 1 not accepted* —
  counted by `standingOf` over what has moved (`block-document-model.md` `BO_0274_006`), read with
  the judgements rather than on a press, since the line carries it at rest. A block reworded or
  clarified is not counted, and one edited with nothing judged of it is counted as edited. *Superseded by «title»* keeps its successor button.
  The line opens the card, and the way in for a root with nothing decided is the bar's.
- *Establish…* stands in the bar's *Document* group (`BO_0274_008`), contributed by the decorating
  extension through the bar contribution of `ui.shell`'s [Layout](../../../../../../docs/system/workspace/layout.md#the-view-bar)
  (`BO_0274_004`), after the view's own actions, and reads *Re-establish…* on a root that already
  stands accepted. It opens the card the line opens. The card keeps its question, its consequences
  and its policy control, and gains the report (`reportLines`, `data-phase-report-line`): every
  block that moved, each by its opening words — *«block» — changed: treated as changing «was» to
  «is now»*, in the refinement's own sentence, *«block» has been edited since the decision, and
  nothing has judged it yet*, and *«block» is not accepted: it contradicts «far claim» in
  «Document»*. On a root already accepted the question is *Re-establish as the accepted direction?*
  and the control reads *Re-establish*, the way a commit command reaches it (`calliopa-refine`'s
  `system.md`, The Commit Convention). *Establish and supersede «title»* stays offered for a
  collision the person means to resolve that way, and no press fails for one.
- Verified on the tree 2026-09-21 (`BO_0274_007`, `BO_0274_008`): `phase-card.test.ts` in the render
  harness — a root nobody decided drawing no line while the bar offers *Establish…*; an accepted
  root's line reading *Accepted · 1 changed, 1 not accepted* with *Re-establish…* in the bar; the
  card asking to re-establish and reporting both kinds in the refinement's own words; and a run's
  phase item accepted and then stamped. `lib/phase.test.ts` proves the words, the counts, the lead's
  two words and the material rule, each shown to bite. The unit project is green at 125 files and
  1095 tests.
- Walked on the served build at pins 976 and 1033, and the user said it works (`BO_0274_009`). The
  walk changed the decision twice. At 976 a document nobody had refined reported nothing, because
  the line counted claims and such a document has none — so the unit became the block and the report
  became the edits a refinement judged material. That alone reported nothing either, since `BO_0258`
  had removed the trigger the same day and nothing judges unasked — so the line gained the free hint
  and the root gained the question that answers it. At 1033 the whole path held: a block edited read
  as *Accepted · 1 block edited since* with nothing running, *Has this moved since it was accepted?*
  raised the refinement that judged it, answering the judgement moved the line to what changed, and
  *Re-establish…* cleared it. It replaces `BO_0249_016`, which walked the surface this change
  removed.

## Proposal Branches

The shell half of `BO_0250` (proposal branches), transferred 2026-09-15 and rescoped 2026-09-16 for a general workplace; the core half is `ccgw.md` (`BO_0250_001`–`BO_0250_004`), the kernel half `ui-kernel.md`, Proposal Branches. A branch is an ordinary open proposal group the person stages into as human class, per document and person, offered on any document the person can edit.

* A proposal is a safe environment: nothing a person does in their branch changes truth until the branch is accepted, and a branch's membership is its group's members, never a visual subtree (`BO_0250`, rescoped 2026-09-16).
* Before a branch is accepted the person sees each member's standing against head — unchanged, moved beneath it and corrected on acceptance, or moved under it — and accepts, rewrites or drops a drifted member; acceptance never needs a run or an extension-specific judgement (`BO_0250`, Decided 2026-09-16).
* A rejected branch loses nothing and leaks nothing: it stays readable as it stood, and a block of it can be promoted on its own into a new proposal (`BO_0250`).
* Working in a branch is offered on any document the person can edit, and a document never refuses a direct edit in favour of a branch. User decision, 2026-09-16.
* No surface marks a member settled: the core's settled mark stays, but no control writes it and no block draws it. User decision, 2026-09-16.

- A person's sessions (`BO_0250_010`, `BO_0250_020`, `CA_0057`; `BranchLine` in `views/branch/branch-line.tsx`, the readings in `lib/branch.ts`): the component stands under the title, before whatever an extension draws on the document, on any document the person can edit — never on a change document, whose members are read-only — whatever its phase or none, and draws nothing there but the acceptance card and a rejected session's rows. The view bar's *Work in a proposal* and the session chips ask it for a session through the editor's store (`branchAsk`), and it writes back the session the tab works in (`branchGroup`), whether someone else accepts (`branchRequired`) and the open sessions (`sessions`), which the bar and the chips read (*The Proposal Toggle And Sessions*, below). It reads `GET d/[id]/branch?account=me` (`fetchBranch`, `documents`' own route) and puts the tab in a group `node:branch-<document>-<account>`, numbered per session, minted by the first staging; the composer's strip says *proposes into your proposal* through the bridge's `setBranch$`. The branch is held per tab and document in the module registry the client helpers consult (`lib/branch-scope.ts`: `branchOf`, `withBranch`, `withBranchBody`), so every read carries `?branch=` and every command `branch` while the tab is in it; the component's store keeps the group across renders and declares the tab on mount and whenever it changes, so a document open twice keeps one session per tab. The editor hands the component its own store and handlers as props, never through `EditorSurfaceContext`: a context value is serialized with the server render, so a tab restored from the workspace resumed holding no document and nothing drew (found on the served build in the walk, 2026-09-16). Entering and leaving deactivate the editor first — a block kept active across the change kept the other scope's revision as its base and its next save was refused as a conflict (found live 2026-09-15) — then read the document, its proposals and the sessions again. At phone width the card and the rejected rows take the headline's gutter (`--block-grip-gutter`), so they begin where the text does. The editor draws the tab's own session as the document it reads, never as proposal rows over it (`shownProposals` skips the group `branchOf` names). A person reading another's document sees truth and, with the proposals toggle, that person's sessions' items in the person's colour (`proposed-changes.md` `BO_0250_016`).
- The acceptance card (`BO_0250_014`, `BO_0250_021`; `data-branch-card` reading `waiting`, `open` or `ready`, the reading `acceptanceReading`): A session chip's *Accept all* enters the session when the tab is not in it, then reads the standing (`GET d/[id]/standing?branch=`, `documents`' own route, `block-document-model.md` `BO_0250_012`), listing each member's — *unchanged*, *moved beneath it, corrected on acceptance*, *moved under it: keep yours, drop it, or rewrite it* (`data-branch-member`, `data-member-standing`). A branch nothing has been staged into yet reads *Nothing is in this proposal yet.* with no *Accept* — the core does not know a group before its first staging, and the card showed its 404 before (found on the served build in the walk, 2026-09-16). With no member drifted the session is accepted at once, without the card standing (`CA_0057_008`); otherwise the card stands under the title with *Accept* (`data-branch-establish`) waiting. A drifted member offers *Keep mine* (`data-branch-keep`) and *Drop* (`data-branch-drop`), its choice on `data-member-choice`, and *Accept* stands only once every drifted member is kept or dropped, the note saying so until then; editing the block in the branch is the rewrite, since the card reads the standing again whenever the document is read again while it is open, and a member no longer drifted loses its choice. *Accept* answers every member of the branch through `answerProposal` in the proposals read's order — a kept member accepted over its drift (`edited`, the override `CA_0042_002` gives a typed-into proposal, which the core admits for a rewrite), a dropped one rejected, the rest accepted — then leaves the session and reads truth; a refused answer is named on the card (`data-branch-refusal`) and the card stands; *Not yet* (`data-branch-not-yet`) closes it. A run that proposed into the branch left its `agent.run` record there as a member, `node:run:<id>` (`isRunRecord`, `runRecordsOf`): the card leaves it out, and *Accept* and *Reject all* answer it with the session (`<group>|run|<ref>`) — the kernel decides a group's own run record without the confirmation page (`ui-kernel.md`, `runRecordMember`) — since the group closes only once every member is answered — before, a branch a run proposed into stayed open after every item was answered, and the next branch could not be named (found on the served build in the walk, 2026-09-16). No run is asked for and no judgement read (`BO_0250`, Decided 2026-09-16). The transition card of `calliopa-refine` follows on its own when the branch carried a `phase` item, since that item is answered as any member.
- The rejected branch and promotion (`BO_0250_015`, `BO_0250_022`): A session chip's *Reject all* enters the session when the tab is not in it, reads the document as the session held it (`fetchDocumentIn`, the overlay read of `ccgw.md` `BO_0250_003`), answers every member rejected through `answerProposal`, leaves the session, and lists under the title what the session held differently from truth — its text blocks that differ or are absent (`rejectedEntries`) — as *Rejected proposal · yours* rows (`data-branch-rejected` the group, `data-rejected-block` each block) with the words it held and *Promote this block on its own* (`data-branch-promote`), which sends `promoteBlock` (`proposed-changes.md` `BO_0250_016`), names the new group in the notice and takes the row away; *Dismiss* (`data-branch-rejected-dismiss`) clears the list, which is the tab's, read at the rejection, and gone on a reload or on entering a session.
- Verified 2026-09-16 on the tree (`BO_0250_018`, `BO_0250_025`): `lib/branch.test.ts` proves the registry, a member's standing in words, the card's reading from the standing and the choices, the branch's members and what a rejected branch held; `views/branch/branch.test.ts` proves the component in the render harness, as `CA_0057_011` records below; `tests/behavior/branch.test.ts` over CCGW under the kernel harness (`TestShellDocumentsOverCCGW`, 22 behaviour files green) proves a branch staged and read through its overlay, a second branch under the next name, the standing drifting under one member, a rejected branch promoting a block, and a branch accepted with a drifted member kept over its drift. The absence check: a copy of the tree without `calliopa-refine` generates its registry, typechecks and passes `documents`' unit tests (22 files, 238 tests); both production builds pass on the full tree.
- Verified with Playwright on the served build, 2026-09-16, pins 1186–1248 (`BO_0250_019`), with two human accounts: at desktop width the line on a clicked-open and on a workspace-restored tab, a rewrite typed in the editor landing as a branch member while truth stood, the other account seeing it as the person's proposal, a drifted member kept with *Keep mine* and *Accept* standing only then, rejection listing the block held and *Promote* staging it into a new proposal, the card of a branch nothing was staged into reading *Nothing is in this proposal yet.*, a run from the tab into an open and into a not-yet-minted branch, and a branch a run proposed into accepted from the card with its run record answered and the next branch named; at 390px the line, the card and the rows on the headline's left edge with no horizontal scroll. The walk found and fixed the context freeze on a restored tab, the unstaged card's 404, the phone gutter's rule order and the run record left undecided (above), and in the kernel the run refused into a not-yet-minted branch and the run record's confirmation page (`ui-kernel.md`, Proposal Branches).

## The Proposal Toggle And Sessions

- Under `ui.shell`'s `CA_0057`, set to draft by the user on 2026-09-18 and transferred here the same day, *Work in a proposal* left the line under the title for a toggle in the view bar, and each proposal session is its own proposal, answered from a chip of its own. The shell's half is `ui.shell`'s [Agent Activity](../../../../../../docs/system/workspace/agent-activity.md), *Proposal Sessions As Chips*. The card and the rejected rows are *Proposal Branches*, above.
* *Work in a proposal* is a toggle in the view bar's trailing *Document* group, before *Delete document*: Phosphor `git-branch`, labelled *Work in a proposal*, pressed while the tab works in one of the person's proposals. It is offered on any document the person can edit, and never on a change document. Pressing it while not pressed starts a new proposal; pressing it while pressed leaves the proposal. User decision, 2026-09-18 (`CA_0057_005`).
* Nothing stands under the title for a session: no *Proposal · yours*, *Accept this proposal*, *Reject this proposal* or *Leave the proposal*. The acceptance card and the rejected rows keep their place under the title, opened from a session chip's answers. User decision, 2026-09-18 (`CA_0057_006`).
* Each proposal session is its own proposal. A session runs from pressing the toggle to pressing it again, and pressing the toggle again starts a new proposal under the next free name, even while earlier ones are still open, rather than reopening the open one. User decision, 2026-09-18 (`CA_0057_007`).
* The view reports one session chip for each of the person's open proposals on the document, beside the run chips. The chip reads *Proposal · yours* followed by the time its session started. Pressing it shows or hides that proposal as a run chip's press does, so several of the person's proposals and any runs' can be shown at once; the pencil beside its words puts the tab in it, so the next edits go into it and the toggle reads as pressed, and a second press on the pencil leaves it. The session the tab works in is the document it reads, so its chip stays shown. User decision in the walk, 2026-09-18 (`CA_0057_014`), replacing the chip's press working in the session. *Accept all* answers it as the card's *Accept* does, and opens the card first when a member has drifted. *Reject all* rejects it and lists the rejected rows, from which a block can still be promoted. User decision, 2026-09-18 (`CA_0057_008`).
- The toggle (`CA_0057_005`) is the `work-in-proposal` action the editor puts first in its `document` bar group, `on` while `branchGroup` is set, left out when the document carries `change`. Its press, and a session chip's pencil and answers, write `branchAsk` (`kind` `toggle`, `work`, `accept` or `reject`, the `group`, a rising `seq`) through the editor's `ask$`, declared before every `$` that calls it; `BranchLine` answers it in a visible task: the toggle enters a new session or leaves; `work` enters the chip's session or, when the tab is in it, leaves; `accept` and `reject` enter the chip's session when the tab is elsewhere, then open the card or reject. The editor routes `answerAll` for a group in `sessions`, and `toggleRun` with `work` (the pencil), to `branchAsk` rather than answering the group as a run's; `toggleRun` without it flips the session's group as a run chip's press does (`toggledGroup`), and does nothing for the session the tab works in. The person's other sessions are drawn as proposal rows over the document while shown, as any group is; only the one the tab works in is drawn as the document (`shownProposalsOf` skips it). BranchLine answers `work` by entering the session or, when the tab is in it, leaving (`CA_0057_014`).
- The session chips (`CA_0057_008`, `CA_0057_014`): `sessionChipsOf` (`lib/branch.ts`) makes one `RunChip` per open session, newest first — `session: true`, the `user` face, `ended` so its answers stand at once, `shown` as `groupShown` says for any group and always while the tab works in it, `working` for that one, and `accepts: "others"` under separation of duties — with `sessionWords`, *Proposal · yours · 14:32* in local time. The editor reports them after `runChipsOf`'s chips, which leave a person's group out.
- The branch read (`CA_0057_009`): `branchOf` (`server/branch.ts`) answers `branch`, the name the next session takes — one past every attempt of the person on the document, open or closed — `sessions`, the open ones newest by attempt, each with `since`, the earliest `createdAt` of its group's revisions, and `previous`, the newest closed one. The read asks `INCLUDE HISTORY`, since a group is revised as stagers join it, and each prior revision answers as an entry of its own under the group's id: the entries are gathered by id, and a group is closed when any of them says `accepted` or `rejected`, since a group only moves from open to closed. A group exists only from its first staging, so a session nothing has been staged into has no chip: the toggle alone shows it, and leaving and entering again before anything is staged takes the same name. `BranchLine` reads it again after entering, leaving and answering, and whenever the editor's proposals were read again, since the first staging mints the group; that read is quiet on failure.
- A block holds one open candidate at a time, the core's rule for its type: a session cannot rewrite, move or retire a block another open session of the person already changed, and the save is refused with `stage_refused` (*already holds an open candidate from another proposal*), which the notice shows in the core's words. A block the other session did not touch, or one it adds, is free.
- [ ] CA_0057 functional question: when a session edits a block an earlier open session of the same person already changed, what should happen? Today the core refuses the save and the notice shows its raw words. Suggested: say it in words — *This block is changed in your proposal of 14:32: work in that one to change it again.* — with that session's chip named, and leave the block as it was. Other ways: move the block's change from the earlier session into the one being worked in, or let the save go into the earlier session.
- Proven (`CA_0057_011`): `views/branch/branch.test.ts` presses in the render harness the toggle in the *Document* group before *Delete* and its absence on a change document, nothing under the title, entering a session through the overlay with the next save naming it and leaving back to truth with the next save based on truth's revision, a session chip with its time and the toggle starting a new session beside it, a chip's pencil working in its session with the toggle pressed, the chip's own press leaving that one shown, and a second press on the pencil leaving; two sessions and a run each shown and hidden by their own chip's press, several at once, while the tab works in neither (`CA_0057_014`); the tab's own session never drawn as proposal rows, *Accept all* with nothing drifted answering every member at once with no reconcile, *Accept all* with drifted members opening the card and *Accept* waiting on each, the standing read again on the next *Accept all* after *Not yet*, a run record answered with the session on *Accept all* and *Reject all*, a refused answer named on the card, and *Reject all* listing the rows with promotion sent; `lib/branch.test.ts` the chip's words and the chips' order, shown and working state and `accepts`. `tests/behavior/branch.test.ts` over CCGW (`TestShellDocumentsOverCCGW`, 24 behaviour files, 118 tests green) proves the read's sessions and next name, an accepted and a rejected session read as closed, and two open sessions of one person on one document, newest first, each read through its own overlay while truth stands, the second refused a rewrite of the block the first holds. The harness's test platform refuses a frame asked while another is pending, which a browser never does, so a press waits for the harness to go quiet and a test waits for its last reads before it ends.
- Walked on the served build at pin 303, 2026-09-18, and the user said it works; the walk decided `CA_0057_014` and `CA_0057_015` (above, and `ui.shell`'s Agent Activity), which were walked at pin 338 the same day, and the user said they work (`CA_0057_012`): several of the person's proposals and a run's shown at once by their chips, the pencil working in one and leaving it, and the grounds of shown chips and of the one worked in.

## Prompts In Place

- Under `calliopa-bootstrap`'s `BO_0267` (2026-09-18), a block sent as a prompt leaves the drawn flow, and the bar's *Show prompts* draws it again ([Block Editor View](./block-editor.md#prompts)).
* The bar's *Show prompts* draws each prompt block where it sits, with a treatment of its own, visibly not proposed, retired or discarded, and a block kept as content after it was sent carries a small prompt mark. With the toggle off neither is drawn. User decision, 2026-09-18.
- A prompt drawn in place is an ordinary block otherwise: it can be activated and edited, and its command control is the one every active block carries, so sending it again is the same *Send*.
- *Show prompts* (`BO_0267_015`) is a toggle in the bar's view group beside *Show discarded blocks*, Phosphor `terminal-window`, off by default. With it on, `readingOrder` keeps prompt blocks in the flow as ordinary rows carrying `data-standing="prompt"` — a dotted card, muted words in the monospace face and the terminal glyph beside `prompt` on its top border (`CardLabel`, `data-card-label`) — and a block that is the `source` of a run the person may see and is not a prompt carries the same label (`data-prompted`), unless it carries a mark of its own, which is the one the label shows. The editor reads which blocks runs were sent from with the document and whenever a run aimed at it ends (`GET /api/runs?artifact=`, `state.sources`), and adds a block it has just sent. With it off neither shows.

## What A Document Shows

* A document remembers what it shows: retired and discarded blocks, proposed changes and prompts come back as the reader left them when the document opens again. User decision, 2026-09-18 (`BO_0267_022`).
- Kept device-locally and per document, as its marks are: `calliopa.shows.<document>` holds the four toggles (`readShows`, `storedShows`, `keepShows` in `views/block-editor.tsx`), written whenever one changes once what the document showed has come back, and cleared when none is on. A browser that keeps nothing opens a document showing nothing extra. It follows the document, not the tab: the same document in another tab opens as it was left.
- Proven in the render harness (`command/command-control.test.ts`): prompts and proposed changes turned on come back on the next opening and are forgotten when turned off, and an unreadable record shows nothing extra.

## Front Matter

- Under `calliopa-bootstrap`'s `BO_0293`, a document's front matter — what a manuscript's head
  projects — is not in this panel: the inspector contributes no action (above), so the front
  matter is edited where it is drawn, in the document's head above the first block
  ([Block Editor View](./block-editor.md#a-manuscript-out-of-the-record)).
