{"status":"success","result":{"roots":["node:calliopa-base","node:calliopa-base.working","node:calliopa-extension","node:calliopa-extension.create-extension","node:settings","node:ui.shell"],"nodes":[{"id":"node:calliopa-base","revision":{"id":"rev:4736a2d40f147d00","tenantId":"local","content":{"_proposal":"node:chg-d6cd27f5882632e9","_type":"ext.manifest","category":"bundled","id":"calliopa-base","version":"0.1.0"},"status":"established","classification":["unclassified"],"schemaVersion":14,"dataRevision":12,"createdAt":1788717068850,"validFrom":1788717068850,"createdBy":"frankzickert","purpose":"BO_0200_015: calliopa-base and calliopa-extension carried from the old graph at pin 4364"},"conflicts":[]},{"id":"node:calliopa-base.working","revision":{"id":"rev:3950c40ab9742d01","tenantId":"local","content":{"_proposal":"node:chg-d6cd27f5882632e9","_type":"ext.skill","conventions":{"atomicProposal":"One extension change is one proposal group: every staged revision and relation belongs to the same group and is accepted or rejected as a unit. Never trickle per-file proposals; a half-applied extension state must never be materializable.","humanWritesCostAConfirmation":"Every human-class write from the shell parks at the kernel bridge and waits for a confirmation on the kernel's own origin — no human mutation travels from the app origin to CCGW. Budget one cross-origin confirmation per write. That is proportionate for occasional truth like creating a channel and disproportionate for continuous editing, so a surface that writes on every keystroke needs a different shape than one that writes on a button.","proofsAreNotProposals":"Never stage a proposal whose intended outcome is rejection. A proof-only group is indistinguishable from a real one at the review surface — the rationale is rendered as ordinary text — so after a run of proposals that all deserved acceptance, one that did not will be accepted by the same motion. Construct demonstrations so the refusal is the whole proof and stop there: a refused mutation stages nothing and opens no group. Acceptance is the one non-delegable human action in the architecture and its entire value is the attention behind it, so anything that produces frequent near-identical almost-always-correct proposals is spending that attention down.","valuesTravelAsParameters":"How a statement says values, which the grammar will not let you guess:\n- Every value beyond a scalar travels beside the statement in the tool call's parameters field, which is one JSON object written as text — for example parameters: \"{\\\"bid\\\": \\\"art-x.b1\\\", \\\"runs\\\": [{\\\"text\\\": \\\"...\\\"}]}\". The statement writes $name where a value belongs; lists and objects have no literal form inside the statement itself, and runs: [{text: ...}] inline is a parse error every time.\n- Every $name in the statement must have a key in that JSON object. An empty parameters value beside a parameterized statement is always wrong, and resending a refused statement without filling it changes nothing.\n- A script is one or more statements separated by semicolons; CREATE ... RELATE ... in one breath is a parse error at RELATE. Aliases scope per statement but parameters are request-global, so one parameters object serves the whole script: the canonical block write is CREATE (n:Text {id: $bid, runs: $runs, order: $order}); RELATE childRef -[r:CONTAINS]-\u003e parentRef, with bid, runs, order, childRef and parentRef all keys of the one JSON object.\n- RELATE endpoint refs resolve from parameters — \u003cref\u003eNodeId first, then \u003cref\u003e — and those values carry the node: prefix (childRef: \"node:art-x.b1\"), even though the id property inside CREATE content is bare. The ref names an existing node; the id property mints one.\n- When syntax is uncertain, calliopa_explain compiles a statement without executing it. Explain and correct rather than retrying a refusal unchanged — the diagnostics name what they want.","writeShapeFollowsFrequency":"Choose the write path by how often the operation happens, not by what it touches. Continuous editing stages candidates with no confirmation and one deliberate act to establish them; an occasional deliberate act — creating a channel, filing something into one — takes the confirmation directly, where the ceremony is proportionate. Both are the same architecture; only the frequency differs, and reaching for one shape everywhere makes typing intolerable or truth too cheap."},"goal":"Stage work into Calliopa's graph as one reviewable proposal, whatever the reader's intention was.","id":"calliopa-base.working","outputContract":"Zero or one open proposal group. Zero is a legitimate outcome and must be said plainly rather than filled with a placeholder.","permittedOperations":["MATCH ... RETURN GRAPH","CREATE","SET","RELATE","RETIRE"],"preconditions":["The run is bound to one proposal group at a pinned dataRevision; nothing staged becomes truth without human acceptance.","The intention's own skill, if the run has one, carries what to produce; this one carries how any of it is staged."]},"status":"established","classification":["unclassified"],"schemaVersion":14,"dataRevision":12,"createdAt":1788717068850,"validFrom":1788717068850,"createdBy":"frankzickert","purpose":"BO_0200_015: calliopa-base and calliopa-extension carried from the old graph at pin 4364"},"conflicts":[]},{"id":"node:calliopa-extension","revision":{"id":"rev:1f05ddfdd07733f5","tenantId":"local","content":{"_proposal":"node:chg-d6cd27f5882632e9","_type":"ext.manifest","capabilities":{"ccgwScopes":["query","propose"],"renders":[]},"category":"bundled","id":"calliopa-extension","version":"1.0.0"},"status":"established","classification":["unclassified"],"schemaVersion":14,"dataRevision":12,"createdAt":1788717068850,"validFrom":1788717068850,"createdBy":"frankzickert","purpose":"BO_0200_015: calliopa-base and calliopa-extension carried from the old graph at pin 4364"},"conflicts":[]},{"id":"node:calliopa-extension.create-extension","revision":{"id":"rev:bc44fd901574daf0","tenantId":"local","content":{"_proposal":"node:chg-26c56898e7df61b5","_type":"ext.skill","conventions":{"authoringRoutes":"Two routes produce the same graph shape; pick one per change and never mix them. (1) Direct CCGW staging — what the examples below show: you write CREATE/SET and stage every partOf and dependsOn relation yourself. (2) The materializer round-trip — kernel checkout into a working tree, edit files, kernel commit — which derives partOf from the tree layout and dependsOn from the manifest dependencies map, so you never write a relation by hand. Human and agent authoring default to the materializer route — the kernel workspace tools are that route for a run; a Block that does not materialize (ext.skill, ext.settings, ext.migration, ext.blocktype, ext.relationtype, ext.intention) is reached by route 1, or carried by a members sidecar on kernel commit --members.","binaryAssets":"Binary files travel: a binary in the diff becomes an ext.source Block whose code is a blob reference, the bytes uploaded through PUT /v1/blobs before the referencing mutation and hash-verified. The shell's favicon, PNGs and font ship this way.","breakingChanges":"Tightening an established definition is gated; loosening it is free. Adding a required property, adding a unique property already duplicated, narrowing permitted children, enabling singleOpenCandidate, renaming, or removing a definition are breaking *when the type has established content* — and only then, so the same change is refused on a populated type and permitted on an empty one. A breaking change needs an ext.migration Block in the same group naming the types it covers, with a rationale and the operations that transform existing content travelling alongside; its authority is the human review of that group, not machinery. Removing a required property needs none, because requiring less invalidates nothing.","capabilities":"The manifest capabilities object carries renders — the surface kinds the extension renders, as plural nouns: routes, views — and ccgwScopes, the CCGW operations its skills need, absent when the extension reads nothing. Nothing validates the shape, so a misspelled capabilities key builds clean and is never noticed.","checkWhereEnforcementRuns":"A declaration is only enforced where something reads it. permittedChildTypes is honoured by the pages layer, and the pages layer is mounted in the production cell rather than the dogfood app — so in a dogfood deployment the declaration is real, resolved, and unenforced. Before relying on any declared constraint, find the code that acts on it and confirm that code is reachable from where the writes happen. When it is not, the surface owns the constraint and the gap belongs in the record.","crossExtensionImports":"The shell imports its extensions and an extension may import the shell's lib and server modules by the ~/ alias; the tree is one pnpm package, so every import resolves and builds. Nothing enforces the manifest's dependsOn against those imports — declare what you import anyway, because the dependency edge is what makes the relationship traversable in the graph.","declaredBaseline":"On the direct CCGW route the mutation must carry dataRevision — the pin the change was computed against. An auto-created proposal group records it as its baseDataRevision, and acceptance conflict-checks against that base. Omit it and the group bases at 0, so acceptance refuses with mutation_resolution_failed conflicts with truth that moved past its base revision 0 — a refusal that names a conflict but is really a missing baseline, and that only appears at acceptance, long after staging looked successful. The materializer route passes the checkout pin for you, which is why this trap belongs to route 1 alone.","definitionShape":"Definition fields are data, not prose. In an ext.blocktype or ext.relationtype Block, requiredProperties, uniqueProperties, permittedChildTypes and runProperties are lists of property-name strings; block and singleOpenCandidate are booleans. A string where a list belongs parses to nothing at all — uniqueProperties: \"name among active channels\" declares no unique property and the rule silently never fires, which is far worse than a refusal because the definition still reads correctly to a human. Put the qualifier in a separate descriptive field, never inside the declaration. The same rule governs the manifest: dependencies is a map of extension id to semver range, capabilities is an object, and category is exactly bundled or individual.","dependencyReferences":"A dependency on another extension is a dependsOn relation from the depending manifest to the dependency manifest, with the semver range recorded in the depending manifest dependencies map keyed by extension id — the range is content because a relation stores no properties, and the relation is what makes the dependency traversable. On the materializer route kernel commit derives the edge from the map; on the direct route you stage it. A dependencies entry naming an extension with no manifest, established or added by the same change, is refused. Two extensions added together may depend on each other.","deploymentIsSeparate":"Acceptance and deployment are two acts. Accepting a proposal establishes truth at a new dataRevision; the kernel keeps serving the promoted release pin until kernel pin --to head moves it, building the candidate before it promotes. An accepted extension is therefore not yet a running one, and nothing warns that the two have diverged — verify the surface appears rather than inferring it from acceptance.","derivedFiles":"Files matching *.gen.* are build output, never content, and the commit path skips them. The tree root's node_modules/, dist/ and server/ are the build's derived directories and are never committed either; a nested src/server/ is ordinary source.","domainVocabulary":"A domain vocabulary is ext.blocktype and ext.relationtype Blocks in an extension of its own, depending on ui.shell — never inside the shell, which stays domain-neutral. Neither Block materializes, so the definitions are staged through direct CCGW or a members sidecar while the extension's sources go through the materializer; that is one rung, two routes, and they must not be mixed inside a single change. Declare uniqueness with uniqueProperties and let the lifecycle carry the qualifier: declared uniqueness is scoped to established revisions, so retiring a node frees its unique values and a \"retired\" or \"archived\" flag kept in content does not, because a flagged node stays established and keeps claiming them.","draftingSurfacesReadThroughTheirDraft":"A surface that stages before establishing has two halves, and the staged half is invisible by design. Staged content is a candidate and does not resolve at head, so every read in a drafting surface must carry the draft’s overlay — the list that finds unoffered drafts, and the read that shows what was just written. Getting this wrong does not produce an error: it produces a dead end, where the author is told to offer a draft they cannot see or reach. Verify that the author can see what they wrote, not only that the write succeeded.","featureExtensions":"A feature extension carries code, not values. The first is settings: src/extensions/settings/ holds the view (settings.tsx), the server modules (server/connections.ts, server/connection-tests.ts, server/adapters.ts) and the feature's migrations (migrations/*.sql, one sequence with the shell's own migrations/ ordered by file name and applied by the tree's serve entry). The shell keeps the thin route files under src/routes/api/settings/ importing from ~/extensions/settings/server/…, and its manifest declares dependencies {\"ui.shell\": \"\u003e=0.1.0\"}. Values the feature stores stay where the feature keeps them — the shell's encrypted connection store — never in ext.settings Blocks, which are plain graph content.","freshNamesAreForced":"A graph definition may never shadow a code-registered type, so the frozen vocabulary’s names are unavailable: Page, Paragraph, Note and Document are all taken. Reincarnating a model therefore means fresh names carrying proven mechanics — which is the intended posture anyway, but the refusal enforces it whether or not you meant it.","migrations":"The tree owns its schema. scripts/lib/migrations.mjs reads migrations/ and every src/extensions/\u003cid\u003e/migrations/ as one sequence ordered by file name, records each in schema_migrations, and the serve entry applies pending ones before it listens — except under the promotion gate's probe (CALLIOPA_SERVE_PROBE=1), which never migrates. Number a new migration after the highest existing one across both places.","naming":"The manifest id is the extension id, lowercase, dot- or hyphen-separated, and must equal its directory name under src/extensions/ — the commit path refuses a mismatch. Source Blocks are \u003cextId\u003e.\u003cpath\u003e. A contribution Block, if one is ever staged, carries a content id equal to its file name.","orderingLivesOnTheChild":"Ordering is a content property on the child block, never on the containment relation, because a Relation stores no properties — a core-model fact, not a design choice. Use a base-36 fractional string compared as an ordinary string, so inserting between two siblings mints a key between theirs and writes one block instead of renumbering the set.","rulesTheGraphCannotExpress":"Some settled rules have no home in the meta-schema and end up enforced in surfaces: which node types are ineligible as a relation’s endpoints, and permitted children wherever the enforcing layer is unreachable. Record each one where the rule is written rather than letting the surface be its only statement, because a rule living only in a surface is a rule the next surface will not know.","shellShape":"The shell of this graph is calliopa-app, imported as ui.shell (BO_0200): a Qwik City application with file-convention routes under src/routes/ and compiled-in views (src/lib/views.ts), root-mapped as a whole. There is no runtime registry, no generated route map and no ext.uicontribution contract in this shell — the kernel still validates contribution Blocks on commit, but nothing reads them. An extension reaches the user through the shell's own source: the shell imports the extension's modules by path (~/extensions/\u003cextId\u003e/…) from a route file under src/routes/, a view entry in src/lib/views.ts, or a server module — so a new feature extension is one proposal that adds the subtree and the import lines in the shell, both root-mapped by the same kernel commit.","structuralVocabularies":"A vocabulary with structure — containment, ordering, permitted children — is a different problem from a vocabulary of flat types. Reuse the code-registered CONTAINS relation rather than declaring a fresh containment type: the block machinery that enforces permitted children and nesting bounds resolves block types through the effective schema, so fresh graph-defined types inherit it, but every containment operation is written against CONTAINS. A fresh containment relation leaves permittedChildTypes declared and inert, which is the silent-no-op failure this system works hardest to avoid.","subtreeShape":"One ext.manifest Block is the subtree root. Every member Block — ext.source, ext.blocktype, ext.relationtype, ext.skill, ext.uicontribution — carries a partOf relation to that manifest.","uiContributionContract":"Retired with the calliopa-app shell. ext.uicontribution Blocks are still a valid member kind the kernel validates on commit (kind route, panel or command; content id equal to the file name), but this shell reads none of them; declare UI through the shell's own imports instead (shellShape).","wholeTreeCommits":"The materializer diff is whole-tree: kernel commit stages everything that differs from the lockfile, not the subtree you were working in. Running the repository's own formatter while authoring an extension can therefore rewrite another extension's sources and stage them alongside your work — observed rewriting className to class across ui.shell. Check kernel status before every commit and confirm the file list is only yours; a foreign file in the list is a signal to stop, not a detail to accept."},"examples":[{"description":"A feature extension the shell imports: the settings extension's shape. One proposal adds the subtree and the shell's import lines.","manifest":{"capabilities":{"renders":["views"]},"category":"bundled","dependencies":{"ui.shell":"\u003e=0.1.0"},"id":"settings","type":"ext.manifest","version":"1.0.0"},"members":[{"code":"the view component, imported by src/components/shell/view-host.tsx as ~/extensions/settings/settings","id":"settings.settings.tsx","language":"tsx","path":"settings.tsx","type":"ext.source"},{"code":"the server module, imported by the route files under src/routes/api/settings/","id":"settings.server/connections.ts","language":"ts","path":"server/connections.ts","type":"ext.source"},{"code":"the feature's schema, applied in sequence with the shell's migrations/","id":"settings.migrations/0006_connection.sql","language":"sql","path":"migrations/0006_connection.sql","type":"ext.source"}],"proposal":"one proposal group staging the subtree, its partOf relations, the dependsOn edge, and the shell's own files that import it — all produced by one kernel commit from the working tree","relations":["settings.settings.tsx partOf settings","settings.server/connections.ts partOf settings","settings.migrations/0006_connection.sql partOf settings","settings dependsOn ui.shell"]},{"description":"A domain vocabulary: two type definitions and their manifest, staged as one proposal. Note that every declaration is a list or a boolean and every human explanation lives in its own field.","manifest":{"capabilities":{"ccgwScopes":["query","propose"],"renders":["views"]},"category":"individual","dependencies":{"ui.shell":"\u003e=0.1.0"},"id":"example.library","type":"ext.manifest","version":"0.1.0"},"members":[{"block":false,"description":"A shelf groups books. Names are unique among active shelves; archival retires the node, which frees the name.","id":"example.library.Shelf","name":"Shelf","requiredProperties":["id","name"],"type":"ext.blocktype","uniqueProperties":["name"]},{"description":"Membership from a book to a shelf. Many-to-many, versioned through ordinary relation validity.","id":"example.library.ON_SHELF","name":"ON_SHELF","requiredProperties":[],"type":"ext.relationtype"}],"proposal":"one proposal group staging the manifest, both definitions, and every relation below, accepted or rejected as a unit","relations":["example.library.Shelf partOf example.library","example.library.ON_SHELF partOf example.library","example.library dependsOn ui.shell"]}],"goal":"Create a new extension, or evolve an existing one, as a complete subtree of Blocks staged in exactly one atomic proposal through CCGW.","id":"calliopa-extension.create-extension","outputContract":"Exactly one open proposal group staging the complete extension change — manifest, member Blocks, their partOf relations, and any dependsOn relations — with rationale and provenance. No writes outside the proposal, no established status, no partial subtree that could materialize half-applied. On the materializer route the same group is produced by kernel commit, with partOf and dependsOn derived rather than written by hand; the resulting graph shape is identical.","permittedOperations":["MATCH ... RETURN GRAPH","CREATE","MERGE","SET","REMOVE","RELATE"],"preconditions":["The extension meta-schema definition Blocks are established truth; read the ext.blocktype and ext.relationtype definitions before staging.","The target extension id is decided and does not collide with an existing ext.manifest id.","Every extension named in the manifest dependencies map has a manifest, established or added by this same change.","The mutation context declares an agent identity scoped to one open proposal group; a skill execution never writes established truth."]},"status":"established","classification":["unclassified"],"schemaVersion":14,"dataRevision":20,"createdAt":1788718027656,"validFrom":1788718027656,"createdBy":"frankzickert","purpose":"BO_0200_016: create-extension skill revised for the calliopa-app shell — no contribution contract, file-convention routes, feature extensions imported by the shell, migrations in sequence, binaries as blobs"},"conflicts":[]},{"id":"node:settings","revision":{"id":"rev:3b7eaf9c153bcfa8","tenantId":"local","content":{"_proposal":"node:chg-3180e1c16ae9059f","_type":"ext.manifest","capabilities":{"renders":["views"]},"category":"bundled","dependencies":{"ui.shell":"\u003e=0.1.0"},"id":"settings","version":"1.0.0"},"status":"established","classification":["unclassified"],"schemaVersion":14,"dataRevision":14,"createdAt":1788717477150,"validFrom":1788717477150,"createdBy":"frankzickert","purpose":"BO_0200_013: calliopa-app as the ui.shell of this graph, with the settings extension"},"conflicts":[]},{"id":"node:ui.shell","revision":{"id":"rev:bd458f95b848b690","tenantId":"local","content":{"_proposal":"node:chg-3180e1c16ae9059f","_type":"ext.manifest","capabilities":{"renders":["routes","views"]},"category":"bundled","id":"ui.shell","version":"0.1.0"},"status":"established","classification":["unclassified"],"schemaVersion":14,"dataRevision":14,"createdAt":1788717477150,"validFrom":1788717477150,"createdBy":"frankzickert","purpose":"BO_0200_013: calliopa-app as the ui.shell of this graph, with the settings extension"},"conflicts":[]}],"relations":[{"id":"rel:04747a9dbfec33c7","type":"partOf","fromNodeId":"node:calliopa-extension.create-extension","to":{"kind":"node","nodeId":"node:calliopa-extension"},"polarity":"positive","dataRevision":12,"createdAt":1788717068850,"validity":{"status":"active","originFromRevisionId":"rev:2039aec85d709c02","targetFromRevisionId":"rev:1f05ddfdd07733f5","establishedBy":"change:045d7866581c8548","dataRevision":12,"updatedAt":1788717068850}},{"id":"rel:e0cd9d80ce94d209","type":"partOf","fromNodeId":"node:calliopa-base.working","to":{"kind":"node","nodeId":"node:calliopa-base"},"polarity":"positive","dataRevision":12,"createdAt":1788717068850,"validity":{"status":"active","originFromRevisionId":"rev:3950c40ab9742d01","targetFromRevisionId":"rev:4736a2d40f147d00","establishedBy":"change:045d7866581c8548","dataRevision":12,"updatedAt":1788717068850}}]},"resolvedDataRevision":20,"diagnostics":[]}
