% IEEE (BO_0293_020): IEEEtran in its conference form, two columns, the IEEE
% bibliography style — the citation style the bibliography already defaults
% to. A Pandoc LaTeX template, as the generic article is.
\documentclass[conference]{IEEEtran}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb}
% A paragraph another sentence refers to is set apart as a numbered remark,
% so the reference has a number to point at (BO_0300_013). LaTeX's own
% \newtheorem, no package.
\newtheorem{remark}{Remark}
% A code block its author numbers is a listing: a float of its own, holding
% the coloured code Pandoc sets, captioned and labelled so a sentence refers
% to it as Listing N (BO_0303_006). float.sty is texlive-latex-recommended's.
\usepackage{float}
\newfloat{listing}{htbp}{lol}
\floatname{listing}{Listing}
\usepackage{graphicx}
\usepackage{longtable,booktabs,array}
\usepackage[font=footnotesize]{caption}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[hidelinks]{hyperref}
% Pandoc sets a code block in colour by itself: it writes \KeywordTok,
% \CommentTok and their kin, and these macros are what define them. No
% Pygments, no minted, and shell escape stays off (BO_0296_008).
\usepackage{fancyvrb}
$highlighting-macros$
\usepackage{listings}
\lstset{basicstyle=\ttfamily\footnotesize,breaklines=true}
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,keepaspectratio}

\title{$title$}
\author{$for(author)$\IEEEauthorblockN{$author.name$}$if(author.affiliation)$\IEEEauthorblockA{$author.affiliation$$if(author.email)$\\$author.email$$endif$}$endif$$sep$ \and $endfor$}

\begin{document}
\maketitle
$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$
$if(keywords)$
\begin{IEEEkeywords}
$for(keywords)$$keywords$$sep$, $endfor$
\end{IEEEkeywords}
$endif$

$body$

$if(natbib)$
$if(bibliography)$
\bibliographystyle{$if(biblio-style)$$biblio-style$$else$IEEEtran$endif$}
\bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}
$endif$
$endif$
$if(supplementary)$
\clearpage
\appendices
\section*{Supplementary Material}
$supplementary$
$endif$
\end{document}
